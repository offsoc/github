---
description: Response when getting cost centers
content:
  application/json:
    schema:
      $ref: '../schemas/get-all-cost-centers.yaml'
    examples:
          default:
            $ref: '../examples/get-all-cost-centers.yaml'
