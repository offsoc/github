---
type: string
description: If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](${externalDocsUrl}/webhooks/event-payloads/#delivery-headers).
example: '"********"'
