---
title: slash command posted event
type: object
properties:
  action:
    type: string
    enum:
      - posted
  command:
    description: The command that was posted.
    type: string
  issue_comment:
    $ref: '../issue-comment.yaml'
required:
  - action
  - command
