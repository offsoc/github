Updated runner <%= view.entry.hit.data[:runner_name] %> <%= "in group #{view.entry.hit.data[:runner_group_name]} " unless view.entry.hit.data[:runner_group_name]&.empty? %>from version <%= view.entry.hit.data[:source_version] %> to version <%= view.entry.hit.data[:target_version] %> with result "<%= view.entry.hit.data[:result]&.downcase %>"
