Previously, a <%= @application_owner %> OAuth application (<%= @authorization.safe_app.name %>) <%= @previous_scopes_message %>. It was recently granted<%= " an" if @added_scopes.count == 1 %> additional <%= pluralize_without_number(@added_scopes.count, "scope") %> (<%= @added_scopes.to_sentence %>). Visit <%= settings_oauth_authorization_url(@authorization.application.key) %> for more information.
