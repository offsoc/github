<%
  # view is an instance of RepositoryAlerts::UpdateError
  page_info(
    title: "Update Error · #{current_repository.name_with_display_owner}",
    container_xl: true,
    selected_link: :alerts,
    dashboard_pinnable_item_id: current_repository.global_relay_id
  )
%>

<%= render layout: "layouts/repository/security", locals: { selected_tab: :dependabot_alerts } do %>
  <%= render "repos/dependabot_alerts/blank_state_update_error", view: view %>
<% end %>
