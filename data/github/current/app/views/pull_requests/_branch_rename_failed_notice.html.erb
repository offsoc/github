<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%# erblint:counter DeprecatedComponentsCounter 1 %>
<% if rename.pull_request_retarget_failed? %>
  <%= render Primer::Beta::Flash.new(scheme: :warning, mt: 3, test_selector: "pr-failed-retarget-after-rename-notice", icon: :alert) do %>
    An attempt to rename this pull request's base branch to '<%= rename.new_name %>'
    did not finish and failed to retarget this pull request to the new branch.
  <% end %>
<% end %>
