<%= render Site::ImageComponent.new(
  src: image_path("modules/site/avatars/#{@handle}.#{@extension}"),
  width: @size*2,
  height: @size*2,
  classes: classes,
  alt: @alt,
  sizes: "#{@size}px",
  loading: @loading,
  style: @style,
  decoding: @decoding,
  "aria-hidden": @aria_hidden,
  **@options
) %>
