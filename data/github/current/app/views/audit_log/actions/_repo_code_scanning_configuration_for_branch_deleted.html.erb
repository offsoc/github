Deleted code scanning configuration <span class="context"><%= view.entry.hit.data[:category].empty? ? "API upload" : view.entry.hit.data[:category] %></span> for branch <span class="context"><%= view.entry.hit.data[:branch] %></span> from <span class="context"><%= view.link_to_repo %></span>
