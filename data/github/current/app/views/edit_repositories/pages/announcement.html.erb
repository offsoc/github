<% page_info \
  container_xl: true,
  title: "Announcement",
  selected_link: :repo_announcements %>

<%= render layout: "edit_repositories/edit" do %>
  <%= render partial: "settings/announcements/announcement_form", locals: {
      banner: banner,
      scope: "repository",
      set_announcement_path: repository_set_announcement_path,
      preview_announcement_path: repository_announcement_preview_path,
      destroy_announcement_path: repository_destroy_announcement_path,
      scope_discussions_path: (discussions_path(current_repository.owner, current_repository) if current_repository.discussions_active?)
    }
  %>
<% end %>
