---
summary: 'List Dependabot alerts for an organization'
description: |-
  Lists Dependabot alerts for an organization.

  The authenticated user must be an owner or security manager for the organization to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
tags:
  - dependabot
operationId: dependabot/list-alerts-for-org

externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/dependabot/alerts#list-dependabot-alerts-for-an-organization'

# Describe how this operation is accessible
# This is essential for us to generate the final OpenAPI description
x-github-internal:
  path: /orgs/{org}/dependabot/alerts
  http-method: get
  service-tier: 2
  published: true
  owner: '@github/dependabot-updates'

# Our OpenAPI description describes all versions of GitHub API
# Define in which versions your operation is available.
x-github-releases:
  - api.github.com
  - ghec
  # ghes accepts a semver range of versions
  - ghes: '>= 3.8'

# Describe any query and/or path parameters this operation
# might receive. Check out existing parameter definitions
# in `app/api/description/components/parameters`
parameters:
  - $ref: '../../components/parameters/org.yaml'
  - $ref: '../../components/parameters/dependabot-alert-comma-separated-states.yaml'
  - $ref: '../../components/parameters/dependabot-alert-comma-separated-severities.yaml'
  - $ref: '../../components/parameters/dependabot-alert-comma-separated-ecosystems.yaml'
  - $ref: '../../components/parameters/dependabot-alert-comma-separated-packages.yaml'
  - $ref: '../../components/parameters/dependabot-alert-scope.yaml'
  - $ref: '../../components/parameters/dependabot-alert-sort.yaml'
  - $ref: '../../components/parameters/direction.yaml'
  - $ref: '../../components/parameters/pagination-before.yaml'
  - $ref: '../../components/parameters/pagination-after.yaml'
  - $ref: '../../components/parameters/pagination-first.yaml'
  - $ref: '../../components/parameters/pagination-last.yaml'
  - $ref: '../../components/parameters/per-page.yaml'

# Now describe what this operation can respond with.
# There is no need in describing common error status codes
# unless they have a specific semantic meaning in your operation.
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/dependabot-alert-with-repository.yaml'
        examples:
          default:
            $ref: '../../components/examples/dependabot-alerts-for-organization.yaml'
  '304':
    $ref: '../../components/responses/not_modified.yaml'
  '400':
    $ref: '../../components/responses/bad_request.yaml'
  '403':
    $ref: '../../components/responses/forbidden.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
  '422':
    $ref: '../../components/responses/validation_failed_simple.yaml'

# Finally, we must tag our operations with some additional metadata
x-github:
  # If the operation is only in GitHub Enterprise Cloud, set this to true
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: dependabot
  subcategory: alerts
