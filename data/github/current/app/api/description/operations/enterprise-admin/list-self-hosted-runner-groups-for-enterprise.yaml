---
summary: List self-hosted runner groups for an enterprise
description: |-
  Lists all self-hosted runner groups for an enterprise.

  OAuth app tokens and personal access tokens (classic) need the `manage_runners:enterprise` scope to use this endpoint.
operationId: enterprise-admin/list-self-hosted-runner-groups-for-enterprise
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/actions/self-hosted-runner-groups#list-self-hosted-runner-groups-for-an-enterprise'
parameters:
  - $ref: '../../components/parameters/enterprise.yaml'
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
  - $ref: '../../components/parameters/visible-to-organization.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: object
          properties:
            total_count:
              type: number
            runner_groups:
              type: array
              items:
                $ref: '../../components/schemas/runner-groups-enterprise.yaml'
          required:
            - total_count
            - runner_groups
        examples:
          default:
            $ref: '../../components/examples/runner-groups-enterprise.yaml'
x-github:
  enabledForGitHubApps: false
  githubCloudOnly: true

  category: actions
  subcategory: self-hosted-runner-groups
x-github-releases:
  - ghec
  - ghes:
      - '>= 2.22'
x-github-internal:
  path: '/enterprises/{enterprise}/actions/runner-groups'
  http-method: get
  service-tier: 1
  published: true
  owner: '@github/c2c-actions'
x-githubEnterpriseOverlays:
  '<= 3.2':
    description: |-
      Lists all self-hosted runner groups for an enterprise.

      OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  '< 3.6':
    parameters:
      - $ref: '../../components/parameters/enterprise.yaml'
      - $ref: '../../components/parameters/per-page.yaml'
      - $ref: '../../components/parameters/page.yaml'
