<ul class="color-border-muted border-top pt-2 mt-2 <%= readonly ? "" : "hx_disabled-input" %>" >
  <%= render(Memex::EditFormFieldComponent.with_collection(columns, memex: memex, values: @column_values, item: item, readonly: readonly)) %>
</ul>
