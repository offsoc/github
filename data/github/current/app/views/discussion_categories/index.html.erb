<% page_info \
  title: "Manage Discussion Categories · #{ is_org_level? ? this_organization.display_login : current_repository.name_with_display_owner }",
  container_xl: true,
  selected_link: :repo_discussions
%>

<%= render layout: "discussions/conditional_discussions_layout" do %>
  <%=
    render(Discussions::CategoriesIndexComponent.new(
      repository: current_repository,
      categories: discussion_categories,
      sections: discussion_sections,
      org_param: is_org_level? ? this_organization.display_login : nil,
    ))
  %>
<% end %>
