<%= render(Primer::Box.new(color: :muted, **system_arguments)) do %>
  Results are based on a
  <%= render(Primer::Beta::Link.new(href: PERMISSIONS_DOC_HREF, muted: true, underline: true)) { "limited selection" } %>
  of repositories.
<% end %>
