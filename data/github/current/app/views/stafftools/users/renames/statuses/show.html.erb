<% page_info title: "Account renamed" %>

<%= render Primer::Beta::Blankslate.new(spacious: true) do |c| %>
  <% c.with_visual_icon(icon: "person") %>

  <% c.with_heading(tag: :h2) do %>
    <%= "Successfully renamed account to #{user.login}." %>
  <% end %>

  <% c.with_primary_action(href: stafftools_user_administrative_tasks_path(user)) do %>
    Continue
  <% end %>
<% end %>
