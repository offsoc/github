<% if view.entry.hit.data[:failure_type] && view.entry.hit.data[:failure_type] == "passkey" %>
  Failed to login with passkey <%= view.entry.hit.data[:note].try(:downcase) %>
<% else %>
  Failed to login <%= view.entry.hit.data[:note].try(:downcase) %>
<% end %>


