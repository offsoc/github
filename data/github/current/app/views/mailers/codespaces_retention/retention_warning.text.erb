Hi @<%= @codespace.owner.display_login %>,

Your codespace "<%= @codespace.display_name %>" is approaching the end of its
retention period and will be deleted on <%= @codespace.retention_expires_at.strftime("%d %b %Y") %> after <%= @codespace.retention_expires_at.strftime("%I:%M%p %Z") %> if you don't take action.

<% if @codespace&.repository.present? %>
  Repository: <%= link_to @codespace.repository.name_with_display_owner, repository_url(@codespace.repository) %>
<% end %>
<% if @codespace.has_unpushed_changes? || @codespace.has_uncommitted_changes? %>

  You have uncommitted or unpushed changes in your codespace.
<% end %>

If you'd like to continue using this codespace, all you need to do is open it by clicking the following link: <%= link_to codespace_url(@codespace), codespace_url(@codespace) %>
