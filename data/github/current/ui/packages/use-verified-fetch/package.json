{
  "name": "@github-ui/use-verified-fetch",
  "description": "A hook which returns a fetch function authenticated by csrf tokens in the react route payload. Consider using the verified-fetch package instead.",
  "main": "use-verified-fetch.ts",
  "exports": {
    ".": "./use-verified-fetch.ts"
  },
  "scripts": {
    "lint": "eslint --cache --max-warnings=0 . --ext .js,.ts,.tsx,.json",
    "test": "jest",
    "tsc": "tsc"
  },
  "private": true,
  "dependencies": {
    "@github-ui/use-csrf-token": "*",
    "react": "*"
  },
  "devDependencies": {
    "@github-ui/eslintrc": "*",
    "@github-ui/jest": "*"
  }
}
