<% if !config_value("github-ssl", "cert").to_s.empty? && !config_value("github-ssl", "key").to_s.empty? -%>
<%= config_value("github-ssl", "cert").chomp %>
<%= config_value("github-ssl", "key").chomp %>
<% end -%>
