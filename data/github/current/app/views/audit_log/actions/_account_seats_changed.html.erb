<% if view.seat_purchase? %>
  Purchased <%= pluralize(view.seat_delta, "seat") %> for
<% else %>
  Removed <%= pluralize(view.seat_delta, "seat") %> from
<% end %>
the <span class="context"><%= view.entry.safe_organization_name %></span> organization
which now has
<%= pluralize(view.entry.hit.data[:new_seats], "total seat") %>
