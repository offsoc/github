---
summary: Sync LDAP mapping for a team
description: Note that this API call does not automatically initiate an LDAP sync.
  Rather, if a `201` is returned, the sync job is queued successfully, and is performed
  when the instance is ready.
operationId: enterprise-admin/sync-ldap-mapping-for-team
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/ldap#sync-ldap-mapping-for-a-team'
parameters:
  - $ref: '../../components/parameters/team-id.yaml'
responses:
  '201':
    description: Response
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: string
        examples:
          default:
            value:
              status: queued
x-github-releases:
  - ghes: '>= 2.18'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: ldap
x-github-internal:
  path: '/admin/ldap/teams/{team_id}/sync'
  http-method: post
  service-tier: 1
  owner: '@github/external-identities'
  published: true
