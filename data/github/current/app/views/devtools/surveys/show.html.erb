<% page_info :title => "#{survey.title}" %>

<% content_for :pagehead_path do %>
  / <%= link_to "Surveys", devtools_surveys_path %>
  / <strong><%= survey.title %></strong>
<% end %>

<%= render :partial => "devtools/surveys/overall_response", locals: { survey: survey } %>
<%= render :partial => "devtools/surveys/answers", locals: { survey: survey } %>
