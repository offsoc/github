<p>Select a repository to link to this package.</p>
<div class="mt-3 d-flex flex-column overflow-hidden">
  <% if owner.feature_enabled?(:search_based_package_linking) %>
    <%= render(Packages::RepositorySearchComponent.new(
      package: package,
      owner: owner,
      repositories: repositories)) %>
  <% else %>
    <%= render(Packages::RepositorySelectionComponent.new(
      package: package,
      owner: owner,
      repositories: repositories)) %>
  <% end %>
</div>
