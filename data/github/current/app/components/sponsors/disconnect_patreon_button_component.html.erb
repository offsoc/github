<%= form_tag(
  sponsors_patreon_users_path(params),
  method: :delete,
) do %>
  <%= render Primer::Beta::Button.new(
    type: :submit,
    test_selector: "disconnect-patreon-button",
  ) do |component| %>
    <% component.with_leading_visual_svg do %>
      <%= render Sponsors::PatreonIconComponent.new %>
    <% end %>

    Unlink Patreon account
  <% end %>
<% end %>
