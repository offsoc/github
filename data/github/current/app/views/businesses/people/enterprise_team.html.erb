<% page_info title: "Enterprise teams · #{enterprise_team["name"]}",
  selected_link: :business_teams,
  stafftools: stafftools_enterprise_path(this_business) %>

<%= render layout: "businesses/layout", locals: {
    business: this_business,
    title: enterprise_team["name"],
    back_to_link_path: enterprise_teams_path(this_business),
    back_to_link_text: "enterprise teams",
  } do %>

  <%= render_react_partial name: "enterprise-team", ssr: true, props: {
    business_slug: this_business.slug,
    enterprise_team:,
    direct_memberships_enabled:,
    members:,
    total_members:,
    member_suggestions_path:,
    page_size:,
    readonly:,
    use_member_organizations_path_for_user_links:,
  } %>
<% end %>
