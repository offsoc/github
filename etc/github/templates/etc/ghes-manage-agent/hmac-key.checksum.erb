<%# HMAC key from `secrets.conf` needed by ghes-manage-agent since we need to ensure we use the one defined for the gateway %>
<% hmac_key = secret_value("ghes-manage", "gateway-agent-hmac-key") %>
<%= Digest::SHA256.hexdigest(hmac_key) %>
