---
title: custom property created event
type: object
properties:
  action:
    type: string
    enum:
      - created
  definition:
    $ref: '../custom-property.yaml'
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - action
  - definition
