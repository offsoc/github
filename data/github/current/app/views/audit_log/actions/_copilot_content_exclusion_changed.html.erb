Copilot content exclusion was changed for
<% if view.org? && !view.repo? %>
  the organization <span class="context"><%= view.link_to_org %></span>
<% elsif view.repo? %>
  the <span class="context"><%= view.link_to_repo %></span> repository
<% elsif view.business? %>
  the <span class="context"><%= view.link_to_business %></span> enterprise
<% end %>
