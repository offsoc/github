# frozen_string_literal: true
# Make sure to have all the dependencies replicated in Gemfile.nightly-ci as well
source "https://rubygems.org"

# Specify your gem's dependencies in scout.gemspec
gemspec :name => "github-scout"

group :debug do
  gem 'byebug' if RUBY_VERSION >= '2.2'
end
