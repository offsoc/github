---
summary: Update LDAP mapping for a user
description: ''
operationId: enterprise-admin/update-ldap-mapping-for-user
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/ldap#update-ldap-mapping-for-a-user'
parameters:
  - $ref: '../../components/parameters/username.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/ldap-mapping-user.yaml'
        examples:
          default:
            $ref: '../../components/examples/ldap-mapping-user.yaml'
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        properties:
          ldap_dn:
            type: string
            description: The [distinguished name](https://www.ldap.com/ldap-dns-and-rdns)
              (DN) of the LDAP entry to map to a team.
        required:
          - ldap_dn
      examples:
        default:
          value:
            ldap_dn: uid=asdf,ou=users,dc=github,dc=com
x-github-releases:
  - ghes: '>= 2.18'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: ldap
x-github-internal:
  path: '/admin/ldap/users/{username}/mapping'
  http-method: patch
  service-tier: 1
  published: true
  owner: '@github/external-identities'
