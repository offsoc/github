---
summary: List public keys
description: ''
operationId: enterprise-admin/list-public-keys
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/users#list-public-keys'
parameters:
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
  - $ref: '../../components/parameters/direction.yaml'
  - name: sort
    in: query
    required: false
    schema:
      type: string
      enum:
        - created
        - updated
        - accessed
      default: created
  - name: since
    description: Only show public keys accessed after the given time.
    in: query
    required: false
    schema:
      type: string
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/public-key-full.yaml'
        examples:
          default:
            $ref: '../../components/examples/enterprise-public-key-items.yaml'
    headers:
      Link:
        $ref: '../../components/headers/link.yaml'
x-github-releases:
  - ghes: '>= 2.18'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: users
x-github-internal:
  path: '/admin/keys'
  http-method: get
  service-tier: 1
  owner: '@github/authentication'
  published: true
