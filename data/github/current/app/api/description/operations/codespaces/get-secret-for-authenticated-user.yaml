---
summary: Get a secret for the authenticated user
description: |-
  Gets a development environment secret available to a user's codespaces without revealing its encrypted value.

  The authenticated user must have Codespaces access to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
tags:
  - codespaces
operationId: codespaces/get-secret-for-authenticated-user
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/codespaces/secrets#get-a-secret-for-the-authenticated-user'
parameters:
  - $ref: '../../components/parameters/secret-name.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/codespaces-secret.yaml'
        examples:
          default:
            $ref: '../../components/examples/user-codespaces-secret.yaml'
x-github-internal:
  path: '/user/codespaces/secrets/{secret_name}'
  http-method: get
  published: true
  owner: '@github/codespaces-team'
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: codespaces
  subcategory: secrets
