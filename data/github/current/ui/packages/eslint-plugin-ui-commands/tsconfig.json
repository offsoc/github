{
  "extends": "@github-ui/tsconfig/base.json",
  "compilerOptions": {
    "rootDir": ".",
    // ESLint requires commonJS - this code is not bundled so we don't use the default `bundler`
    "moduleResolution": "Node",
    "checkJs": true
  },
  "exclude": ["jest.config.js", ".eslintrc.js"]
}
