---
type: object
properties:
  status:
    $ref: './ghes-cluster-status-indicator.yaml'
  nodes:
    type: array
    items:
      type: object
      properties:
        hostname:
          type: string
        status:
          $ref: './ghes-cluster-status-indicator.yaml'
        services:
          type: array
          items:
            type: object
            properties:
              status:
                $ref: './ghes-cluster-status-indicator.yaml'
              name:
                type: string
              details:
                type: string
