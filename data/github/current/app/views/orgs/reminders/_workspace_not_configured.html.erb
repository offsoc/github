<p class="flash flash-warn js-reminder-client-error <%= client_class %>" <% if hidden %> hidden <% end -%>>
  <%# 'More details here' link to documentation not final yet %>
  <% if this_organization.adminable_by?(current_user) %>
    Before using scheduled reminders, <%= client_name %> platform has to be configured for this organization. More details here.
  <% else %>
    Before using scheduled reminders, an organization admin needs to configure <%= client_name %> platform for <%= this_organization.display_login %>. More Details here.
  <% end %>
</p>
