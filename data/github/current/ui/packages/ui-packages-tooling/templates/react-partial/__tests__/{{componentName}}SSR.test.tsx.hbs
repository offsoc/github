/** @jest-environment node */
import {serverRenderReact} from '@github-ui/ssr-test-utils/server-render'
import {get{{componentName}}Props} from '../test-utils/mock-data'

// Register with react-core before attempting to render
import '../{{#if enableSSR}}ssr-{{/if}}entry'

test('Renders {{packageName}} partial with SSR', async () => {
  const props = get{{componentName}}Props()
  const view = await serverRenderReact({
    name: '{{packageName}}',
    data: {props},
  })

  // verify ssr was able to render some content from the props
  expect(view).toMatch(props.exampleMessage)
})
