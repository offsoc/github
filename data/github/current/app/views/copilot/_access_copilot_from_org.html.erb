<% if current_user.member_or_billing_manager_for_any_organization? %>
  <%= render(Hydro::TrackViewComponent.new(name: "copilot-trial-signup-get-copilot-from-organization")) %>
  <%= render(Growth::BannerComponent.new(icon: :organization, p: 3, mt: 3)) do |component| %>
    <% component.with_title do %>
      Or get GitHub Copilot from an organization
    <% end %>
    <% component.with_description do %>
      Organizations can provide their members (including you) and their teams access to GitHub Copilot.
      <%= render(Primer::Beta::Link.new(
        href: copilot_settings_path,
        data: analytics_click_attributes(
          category: "copilot_trial_signup",
          action: "click_to_view_copilot_settings",
          label: "ref_cta:access_copilot_from_an_organization;ref_loc:pay_frequency_after_trial;"
        ),
        classes: "Link--inTextBlock"
      )) do %>
        Access Copilot from an organization
      <% end %>
      .
    <% end %>
  <% end %>
<% end %>
