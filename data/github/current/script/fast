if [ $# -eq 0 ]; then
  bin/rake -f test/fast/Rakefile test:fast
else
  if [[ $1 == -* ]]; then
    bin/rake -f test/fast/Rakefile test:fast $1 $2 $3 $4 $5
  else
    bin/rake -f test/fast/Rakefile test:fast:$1 $2 $3 $4 $5
  fi
fi

