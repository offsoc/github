---
summary: Set announcement banner for organization
description: Sets the announcement banner to display for the organization.
tags:
  - orgs
operationId: announcement-banners/set-announcement-banner-for-org
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/announcement-banners/organizations#set-announcement-banner-for-organization'
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/announcement.yaml'
      examples:
        default:
          $ref: '../../components/examples/announcement.yaml'

parameters:
  - $ref: '../../components/parameters/org.yaml'

responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/announcement-banner.yaml'
        examples:
          default:
            $ref: '../../components/examples/announcement.yaml'

x-github-releases:
  - ghes: '>= 3.8'
  - ghec

x-github:
  githubCloudOnly: true
  enabledForGitHubApps: true
  category: announcement-banners
  subcategory: organizations

x-github-internal:
  path: /orgs/{org}/announcement
  http-method: patch
  owner: '@github/repos'
  published: true
