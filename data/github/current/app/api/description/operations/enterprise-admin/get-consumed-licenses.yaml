---
summary: 'List enterprise consumed licenses'
description: |-
  Lists the license consumption information for all users, including those from connected servers, associated with an enterprise.

  The authenticated user must be an enterprise admin to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
tags:
  - enterprise-admin
operationId: enterprise-admin/get-consumed-licenses

externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/license#list-enterprise-consumed-licenses'

x-github-resource-owner: '@github/licensing'

x-github-internal:
  path: /enterprises/{enterprise}/consumed-licenses
  http-method: get
  published: true
  owner: '@github/licensing'

x-github-releases:
  - ghec

parameters:
  - $ref: '../../components/parameters/enterprise.yaml'
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'

responses:
  '200':
    description: Consumed Licenses Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/get-consumed-licenses.yaml'
        examples:
          default:
            $ref: '../../components/examples/get-consumed-licenses.yaml'
    headers:
      Link:
        $ref: '../../components/headers/link.yaml'

x-github:
  githubCloudOnly: true
  enabledForGitHubApps: false
  previews: []
  category: enterprise-admin
  subcategory: license
