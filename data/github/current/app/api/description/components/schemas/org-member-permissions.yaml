---
title: Org Member Permissions
description: The permissions a user has on an organization.
type: object
properties:
  permissions:
    type: array
    description: The permissions the user has in the organization.
    items:
      type: string
  membership:
    type: string
    description: The user's membership type in the organization.
    enum:
      - member
      - admin
      - suspended
    example: admin
