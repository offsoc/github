<%= render GitHub::BatchDeferredContentComponent.new(
  url: ref_ahead_behind_path,
  inputs: { base_commit: branch.base_commit, head_commit: branch.commit }
  ) do %>
  <%= render Primer::Beta::Spinner.new(size: :small) %>
<% end %>
