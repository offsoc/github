No new secrets detected in your historical scan of <b><%= @org_or_enterprise_name %></b>

We performed a historical scan for eligible repositories in <b><%= @org_or_enterprise_name %></b> and found no secrets.

If we find any new secrets in your <%= @org_or_enterprise_type %>, you can view the alerts in <%= @alerts_text%>: <%= @alerts_url %>.
