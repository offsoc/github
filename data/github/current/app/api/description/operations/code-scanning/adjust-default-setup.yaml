---
summary: Adjust a code scanning default setup configuration
description: |-
  Adjusts a code scanning default setup configuration.

  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
tags:
  - code-scanning
operationId: code-scanning/adjust-default-setup
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/code-scanning/code-scanning#adjust-a-code-scanning-default-setup-configuration'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/code-scanning-default-setup-adjust.yaml'
      examples:
        default:
          $ref: '../../components/examples/code-scanning-default-setup-adjust.yaml'
responses:
  '202':
    description: Success response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/empty-object.yaml'
        examples:
          default:
            value:
  '403':
    $ref: '../../components/responses/code_scanning_forbidden_write.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
  '503':
    $ref: '../../components/responses/service_unavailable.yaml'
x-github-releases:
  - ghec
  - api.github.com
  - ghes:
      - '>= 3.12'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: code-scanning
  subcategory: code-scanning
x-github-internal:
  path: '/repos/{owner}/{repo}/code-scanning/default-setup/adjust'
  http-method: patch
  service-tier: 2
  owner: '@github/code-scanning-experiences'
  published: false
  deprecatedFields: false
