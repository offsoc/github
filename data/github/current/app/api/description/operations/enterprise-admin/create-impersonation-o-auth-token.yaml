---
summary: Create an impersonation OAuth token
description: ''
operationId: enterprise-admin/create-impersonation-o-auth-token
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/users#create-an-impersonation-oauth-token'
parameters:
  - $ref: '../../components/parameters/username.yaml'
responses:
  '201':
    description: Response when creating a new impersonation OAuth token
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/authorization.yaml'
        examples:
          default:
            $ref: '../../components/examples/authorization.yaml'
  '200':
    description: Response when getting an existing impersonation OAuth token
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/authorization.yaml'
        examples:
          default:
            $ref: '../../components/examples/authorization.yaml'
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        properties:
          scopes:
            type: array
            description: A list of [scopes](${externalDocsUrl}/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
            items:
              type: string
        required:
          - scopes
      examples:
        default:
          value:
            scopes:
              - public_repo
x-github-releases:
  - ghes: '>= 2.18'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: users
x-github-internal:
  path: '/admin/users/{username}/authorizations'
  http-method: post
  service-tier: 1
  owner: '@github/meao'
  published: true
