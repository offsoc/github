<%= render(ProgrammaticAccess::PreviewMarkdownComponent.new(
  data_preview_url: preview_grant_request_reason_path(target_name: current_target.display_login),
  field: :reason, name: :reason, maxlength: 1024,
  placeholder: "Describe why your personal access token needs access to the #{current_target.display_login} organization.",
  text_content: request_reason
)) %>
