<%= @header %> <%= @repo.name_with_display_owner %>:

Please resolve these alerts
<%= @message %>

<% @alerts.take(@alerts_list_limit).each do |alert| %>
  <%= alert.label %>
  <%= alert.first_location_text %>
  Review at <%= GitHub.url %><%= GitHub.url %>/<%= @repo.name_with_display_owner %>/security/secret-scanning/<%= alert.number %>

<% end %>
<% if @alerts_list_limit < @total_scan_count %>
and <%= @total_scan_count - @alerts_list_limit %> more alerts
<% end %>

<% if @detected_by_ai %>
These secrets were detected by AI. False positive rates will improve over time.
<% end %>

You are receiving this email because GitHub scanned for exposed tokens or private key credentials in your repository.

Thanks,
GitHub.com
