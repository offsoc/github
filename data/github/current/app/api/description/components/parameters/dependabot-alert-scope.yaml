---
name: scope
in: query
description: |-
  The scope of the vulnerable dependency. If specified, only alerts with this scope will be returned.
schema:
  type: string
  enum:
    - development
    - runtime
