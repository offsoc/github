---
summary: Set announcement banner for repository
description: Sets the announcement banner to display for the repository.
tags:
  - repos
operationId: announcement-banners/set-announcement-banner-for-repo
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/announcement-banners/repositories#set-announcement-banner-for-repository'
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/announcement.yaml'
      examples:
        default:
          $ref: '../../components/examples/announcement.yaml'

parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'

responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/announcement-banner.yaml'
        examples:
          default:
            $ref: '../../components/examples/announcement.yaml'

x-github-releases:
  - ghes: '>= 3.8'
  - ghec

x-github:
  githubCloudOnly: true
  enabledForGitHubApps: true
  category: announcement-banners
  subcategory: repositories

x-github-internal:
  path: /repos/{owner}/{repo}/announcement
  http-method: patch
  owner: '@github/repos'
  published: false
