<%= render(Primer::Alpha::SelectPanel.new(
  id: "actions_status_filter",
  title: "Filter by Status",
  no_results_label: "No matching statuses.",
  src: actions_statuses_menu_path(selected_filename: selected_workflow&.filename, lab: selected_workflow&.lab?, query: actions_filtered_query(filters: workflow_run_filters))
)) do |menu| %>
  <% menu.with_show_button(scheme: :invisible, color: :muted, font_weight: :normal) do |button| %>
    <% button.with_trailing_action_icon(icon: :"triangle-down", display: [:none, :block]) %>
      Status
  <% end %>
<% end %>
