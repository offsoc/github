<%= render component do %>
  <%= primer_octicon(:info, size: :xsmall, mr: 1, color: :accent) if tiers_page? %>
  Some of your Patreon tiers will not be available on GitHub because they do not meet
  <% if tiers_page? # shown right below the min amount, so we don't need to reiterate it %>
    this minimum.
  <% else %>
    your minimum amount of <%= min_custom_amount_money.format(no_cents_if_whole: true) %>.
  <% end %>
<% end %>
