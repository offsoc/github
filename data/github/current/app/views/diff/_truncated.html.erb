<%# locals: GitHub::Diff::Entry diff_entry %>
<% case diff_entry.truncated_reason
   when "maximum diff size exceeded.", "maximum number of lines exceeded." %>
  Sorry, we could not display the changes to this file because there were too many other changes to display.
<% when /^maximum file count exceeded/ %>
  Sorry, we could not display the changes to this file because there were too many other files to display.
<% else %>
  <% if diff_entry.truncated_reason.present? %>
    <%= diff_entry.truncated_reason %>
  <% else %>
    Sorry, we could not display the changes to this file.
  <% end %>
<% end %>
