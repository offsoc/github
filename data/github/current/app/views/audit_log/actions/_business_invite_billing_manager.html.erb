<% if view.email_invitation? %>
  Invited <span class="context"><%= view.entry.hit.data[:email] %></span> to be a billing manager for the <span class="context"><%= view.entry.safe_business_name %></span> enterprise
<% else %>
  Invited <%= view.link_to_user %> to be a billing manager for the <span class="context"><%= view.entry.safe_business_name %></span> enterprise
<% end %>
