version: "3.6"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ARG_GITHUB_USERNAME: $GITHUB_USERNAME
        ARG_BUNDLE_RUBYGEMS__PKG__GITHUB__COM: $BUNDLE_RUBYGEMS__PKG__GITHUB__COM
