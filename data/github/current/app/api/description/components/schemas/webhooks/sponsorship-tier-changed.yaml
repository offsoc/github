---
title: sponsorship tier_changed event
type: object
properties:
  action:
    type: string
    enum:
      - tier_changed
  changes:
    $ref: ../webhooks_changes_8.yaml
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  repository:
    $ref: '../repository-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
  sponsorship:
    $ref: ../webhooks_sponsorship.yaml
required:
  - action
  - changes
  - sponsorship
  - sender
