---
title: security_advisory updated event
type: object
properties:
  action:
    type: string
    enum:
      - updated
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  repository:
    $ref: '../repository-webhooks.yaml'
  security_advisory:
    $ref: ../webhooks_security_advisory.yaml
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - action
  - security_advisory
