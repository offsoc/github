<%= render Primer::Alpha::Dialog.new(
  title: "Create status badge",
  size: :medium_portrait,
  id: dialog_id,
) do |d| %>
  <% d.with_body do %>
    <include-fragment loading="lazy" src="<%=src_path%>">
      <%= render(Primer::Beta::Spinner.new(my: 3, mx: :auto, display: :block)) %>
    </include-fragment>
  <% end %>
<% end %>
