<%= render(GitHub::FlashActionDismissibleComponent.new(
  level: :warning,
  dismissible_path: dismissal_path,
  dismissible_method: dismissal_method,
  test_selector: "ip-allowlist-ipv6-notice",
  mb: 4)) do |c| %>
  <% c.with_title do %>
    IP allow lists and IPv6
  <% end %>
  <% c.with_text do %>
    GitHub is gradually rolling out IPv6. To prevent possible access interruptions with
    your <%= owner_type %>, please ensure you have added any necessary IPv6 addresses to
    your IP allow list.
    <%= render(Primer::Beta::Link.new(href: learn_more_url, classes: "Link--inTextBlock")) { "Learn more" } %>.
  <% end %>
<% end %>
