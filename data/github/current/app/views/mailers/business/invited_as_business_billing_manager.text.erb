Hi<%= " #{@user.safe_profile_name}" if @user.present? %>,

<% if @show_inviter %>@<%= @inviter.display_login %> has invited you<% else %>You’ve been invited<% end %> to be a billing manager for the <%= @business.name %> enterprise on GitHub.

This invitation will expire in <%= pluralize(GitHub.invitation_expiry_period, "day") %>.

You will be able to perform the following actions for <%= @business.name %>:

- View the billing usage for the enterprise

Please follow this link to confirm and accept the invitation:

<%= @url %>

If you were not expecting this invitation, you can ignore this email.

Thanks,
The GitHub Team
