export const nextSteps = `
Next steps:

- \`npm run jest:watch\` - this will start the jest tests and run only the files which have changed
- \`git add\` files and run \`script/generate-service-files.rb\`
`
