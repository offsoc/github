<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%= render Primer::IconButton.new(
        tag: :a,
        icon: :pencil,
        "aria-label": "Edit: #{@release.display_name}",
        href: edit_release_path(@release, @repository),
        test_selector: "edit-button",
        **@system_arguments
      )
%>
