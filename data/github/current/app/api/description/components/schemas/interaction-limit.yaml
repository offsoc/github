---
title: Interaction Restrictions
description: Limit interactions to a specific type of user for a specified duration
type: object
properties:
  limit:
    $ref: './interaction-group.yaml'
  expiry:
    $ref: './interaction-expiry.yaml'
required:
  - limit
