<%= content_tag content_tag_name, class: classes, "data-url": url, **content_tag_options do %>
  <% inputs.each do |name, value| %>
    <%= hidden_field_tag name, value, id: nil, "data-targets": "#{content_tag_name}.inputs" %>
  <% end %>

  <%= content %>
<% end %>
