---
title: workflow_dispatch event
type: object
properties:
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  inputs:
    type: object
    nullable: true
    additionalProperties: true  # this is actual customer data in variable form
  installation:
    $ref: '../simple-installation.yaml'
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  ref:
    type: string
  repository:
    $ref: '../repository-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
  workflow:
    type: string
required:
  - inputs
  - ref
  - repository
  - sender
  - workflow
