
<%= render(Primer::BaseComponent.new(id: @id, **@kwargs)) { content } %>
<% if render_tooltip? %>
  <%= render Primer::Alpha::Tooltip.new(for_id: @id, type: :description, text: @description) %>
<% end %>
