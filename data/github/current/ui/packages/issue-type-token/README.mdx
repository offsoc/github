import {Canvas, Meta, Story} from '@storybook/blocks'
import {Controls} from '@storybook/blocks'
import * as IssueTypeTokenStories from './IssueTypeToken.stories'
import {IssueTypeToken} from './IssueTypeToken'

<Meta of={IssueTypeTokenStories} />

# IssueTypeToken

{/* A precise description of your component and the use cases it covers. */}

This is a component that uses Primer's `Token` component to display an issue type. If the name of the type is too long, it will be truncated.
It contains a link that is required, and an optional tooltip text to show.

## Example

A simple Bug token

<Canvas of={IssueTypeTokenStories.BugToken} />

A token with a long name and tooltip on hover

<Canvas of={IssueTypeTokenStories.TokenWithLongName} />

A token that shows the description on hover

<Canvas of={IssueTypeTokenStories.TokenWithHoverDescription} />

## Properties

{/* You can use the `ArgsTable` component to display a table of properties once you have components. Make sure to import them. */}

<Controls of={IssueTypeTokenStories.BugToken} />

{' '}
