Hey @<%= @user.display_login %>,

Your <%= @token_type %> "<%= @oauth_access.description %>"<% if @oauth_access.scopes && @oauth_access.scopes.any? %> with <%= html_safe_to_sentence(@oauth_access.scopes) %> <%= pluralize_without_number(@oauth_access.scopes.count, "scope") %><% end %> has expired.

If this token is still needed, visit <%= @reset_link %> to generate an equivalent.

If you run into problems, please contact support by visiting <%= contact_url %>

Thanks,
The GitHub Team
