<%# View is an Oauth::AuthorizeIntegrationView %>
<%= @application_description %> with new permissions (<%= @view.name %>) was re-authorized to:
<% @view.permissions_added.merge(@view.permissions_upgraded).each do |resource, action| %>
  - <%= @view.description(resource) %>
<% end %>

Visit <%= settings_oauth_authorization_url(@view.key) %> for more information.
