<% if show_policy_codespaces_limit %>
  <% if show_org_policy_codespaces_limit %>
    Limit of <%= policy_codespaces_limit %> <%= "codespace".pluralize(policy_codespaces_limit) %> based on a policy set by <%= render(Primer::Beta::Text.new(font_weight: :bold)) { codespaces_limit_policy_owner } %> is reached. Delete an existing codespace owned by <%= render(Primer::Beta::Text.new(font_weight: :bold)) { codespaces_limit_policy_owner } %> to create a new one.
  <% else %>
    Limit of <%= policy_codespaces_limit %> <%= "codespace".pluralize(policy_codespaces_limit) %> based on a policy set by <%= render(Primer::Beta::Text.new(font_weight: :bold)) { codespaces_limit_policy_owner } %> is reached. To create a new codespace billable to <%= render(Primer::Beta::Text.new(font_weight: :bold)) { billable_owner.display_login } %>, delete an existing codespace owned by any organization within the <%= render(Primer::Beta::Text.new(font_weight: :bold)) { codespaces_limit_policy_owner } %> enterprise.
  <% end  %>
<% else %>
  Limit of <%= user_codespace_limit %> <%= "codespace".pluralize(user_codespace_limit) %> reached. Delete an existing codespace to create a new one.
<% end %>
