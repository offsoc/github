# This is a pseudo-package that exists simply to disable dependency enforcement for transitions until we
# have a path to migrating them into their respective packages. The noise from the dependency violations
# that are unactionable is too high to be useful.

enforce_dependencies: false
enforce_privacy: false
