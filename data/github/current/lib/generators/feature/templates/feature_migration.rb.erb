# frozen_string_literal: true

require "github/transitions/create_<%= file_name %>_feature"

class RunCreate<%= class_name %>FeatureTransition < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version %>]
  def self.up
    return if !GitHub.enterprise? && !Rails.env.development?
    transition = GitHub::Transitions::Create<%= class_name %>Feature.new(dry_run: false)
    transition.perform
  end

  def self.down
  end
end
