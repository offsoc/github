<%
  page_info(
    title: "Security Policy · #{current_repository.name_with_display_owner}",
    container_xl: true,
    selected_link: :policy,
    dashboard_pinnable_item_id: current_repository.global_relay_id
  )
%>

<%= render layout: "layouts/repository/security", locals: { selected_tab: :policy } do %>
  <%= render(Repos::Security::PolicyComponent.new(repository: current_repository)) %>
<% end %>
