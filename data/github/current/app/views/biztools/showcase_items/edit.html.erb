<%
  page_info title: "#{item.item.name_with_owner} - Edit - #{collection.name} - Showcase · Biztools",
            selected_link: :showcase_biztools
%>
<%= render layout: "biztools/biztools" do %>
  <%= render Stafftools::Explore::ShowcaseItems::FormComponent.new(
    showcase: collection,
    item: item,
    context: :biztools,
  ) %>
<% end %>
