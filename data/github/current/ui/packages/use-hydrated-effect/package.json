{
  "name": "@github-ui/use-hydrated-effect",
  "main": "use-hydrated-effect.ts",
  "description": "An SSR-Friendly wrapper around `useLayoutEffect` that will not run on the server and so will not trigger the React warning about calling `useLayoutEffect` on the server.",
  "exports": {
    ".": "./use-hydrated-effect.ts"
  },
  "scripts": {
    "lint": "eslint --cache --max-warnings=0 . --ext .js,.ts,.tsx,.json",
    "test": "jest",
    "tsc": "tsc"
  },
  "private": true,
  "dependencies": {
    "@github-ui/ssr-utils": "*",
    "react": "*"
  },
  "devDependencies": {
    "@github-ui/eslintrc": "*",
    "@github-ui/jest": "*"
  }
}
