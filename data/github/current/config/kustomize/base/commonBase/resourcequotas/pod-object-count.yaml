apiVersion: v1
kind: ResourceQuota
metadata:
    name: pod-object-count
    namespace: base-namespace
spec:
    hard:
        # count/pods is total object count for pods, regardless of status
        count/pods: "2000"
