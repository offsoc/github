<%= @content.text %> for the <%= account_description(@owner) %>.

<% if !@owner.try(:has_any_trade_restrictions?) && !@owner.delegate_billing_to_business? %>
  <%= sanitize_html(@content.action_text) %>
  Visit <%= target_billing_url(@owner, tab: :spending_limit) %> to find out how.
<% end %>
