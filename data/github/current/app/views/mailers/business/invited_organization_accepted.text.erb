Hello!

@<%= @actor.display_login %> has accepted the invitation for <%= @organization.name %> to join the <%= @business.name %> enterprise on GitHub.

<% if @business.trial? %>
Since <%= @business.name %> is a trial enterprise account,<%- if @organization.has_valid_payment_method? %><%= @organization.name %> organization billing will pause and only resume if the trial account expires or is cancelled. Additionally,<%- end %> certain features are unavailable during trial.
Learn more at https://docs.github.com/get-started/signing-up-for-github/setting-up-a-trial-of-github-enterprise-cloud#features-not-included-in-the-trial
<% end %>

To complete the transfer of ownership to <%= @business.name %>, follow this link:

<%= pending_organizations_enterprise_url(@business) %>

This invitation will expire in <%= pluralize(BusinessOrganizationInvitation::INVITATION_EXPIRY_PERIOD, "day") %>.

Some helpful tips:

- If you get a 404 page, make sure you’re signed in as an owner of <%= @business.name %>.
- You can also confirm the invitation by visiting the enterprise page directly at <%= enterprise_url @business %> and clicking the "Pending" button.

Thanks,
The GitHub Team
