Enabled notification restrictions for the
<span class="context">
  <% if view.org? %>
    <%= view.link_to_org %> organization.
  <% else %>
    <%= view.link_to_business(view.entry.hit[:owner]) %> enterprise.
  <% end %>
</span>
