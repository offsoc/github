<%# erblint:counter DeprecatedComponentsCounter 2 %>
<% repos.each do |repo| %>
    <%= render Primer::Alpha::AutoComplete::Item.new(
      value: repo,
      border: :bottom,
      "data-name": repo,
    ) do %>
    <%= repo %>
  <% end %>
<% end %>
