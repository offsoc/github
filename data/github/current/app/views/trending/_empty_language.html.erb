<% suffix = " for #{Linguist::Language[language] || language}" if language %>

<%= render Primer::Beta::Blankslate.new do |c| %>
  <% c.with_heading(tag: :h2) do %>
    <%= "It looks like we don’t have any trending #{list_type}#{suffix}." %>
  <% end %>
<% end %>
