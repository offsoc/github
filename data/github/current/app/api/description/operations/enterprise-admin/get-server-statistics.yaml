---
summary: Get GitHub Enterprise Server statistics
description: |-
  Returns aggregate usage metrics for your GitHub Enterprise Server 3.5+ instance for a specified time period up to 365 days.

  To use this endpoint, your GitHub Enterprise Server instance must be connected to GitHub Enterprise Cloud using GitHub Connect. You must enable Server Statistics, and for the API request provide your enterprise account name or organization name connected to the GitHub Enterprise Server. For more information, see "[Enabling Server Statistics for your enterprise](/admin/configuration/configuring-github-connect/enabling-server-statistics-for-your-enterprise)" in the GitHub Enterprise Server documentation.

  OAuth app tokens and personal access tokens (classic) need:
    - the `read:enterprise` scope if you connected your GitHub Enterprise Server to an enterprise account and enabled Server Statistics
    - the `read:org` scope if you connected your GitHub Enterprise Server to an organization account and enabled Server Statistics
operationId: enterprise-admin/get-server-statistics
tags:
  - server-statistics
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/admin-stats#get-github-enterprise-server-statistics'
parameters:
  - $ref: '../../components/parameters/enterprise-or-org.yaml'
  - name: date_start
    description: 'A cursor, as given in the [Link header](${externalDocsUrl}/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events after this cursor.'
    in: query
    required: false
    schema:
      type: string
  - name: date_end
    description: 'A cursor, as given in the [Link header](${externalDocsUrl}/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events before this cursor.'
    in: query
    required: false
    schema:
      type: string
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/server-statistics.yaml'
        examples:
          default:
            $ref: '../../components/examples/server-statistics.yaml'
x-github-releases:
  - ghec
x-github:
  githubCloudOnly: true
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: admin-stats
x-github-internal:
  path: '/enterprise-installation/{enterprise_or_org}/server-statistics'
  http-method: get
  service-tier: 1
  published: true
  owner: "@github/meao"
