<%# View is an Oauth::AuthorizeIntegrationView %>
<%= @application_description %> (<%= @view.name %>) with the following permissions:

<% @view.permissions.each do |resource, action| %>
  - <%= @view.description(resource) %>
<% end %>

Was recently authorized to access your account.

Visit <%= settings_oauth_authorization_url(@view.key) %> for more information.
