Description:
    Stubs out a new EventData platform object. Pass the action name, either
    CamelCased or under_scored, and an optional list of attribute pairs as
    arguments.

    Attribute pairs are field:type arguments specifying the model's queryable
    fields. Available field types are all platform objects types. If no type
    is specified the string type will be used by default.

Example:
    rails generate audit_log_event_data "org.create" organization_name:String organization_database_id:Integer

    Creates an EventData object called OrgLoginData with two fields string
    organization_name and integer organization_database_id at:
        app/platform/objects/audit_log/org_create_event_data.rb
