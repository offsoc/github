---
summary: Get GitHub Actions permissions for a repository
description: |-
  Gets the GitHub Actions permissions policy for a repository, including whether GitHub Actions is enabled and the actions and reusable workflows allowed to run in the repository.

  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
operationId: actions/get-github-actions-permissions-repository
tags:
  - actions
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/actions/permissions#get-github-actions-permissions-for-a-repository'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/actions-repository-permissions.yaml'
        examples:
          default:
            $ref: '../../components/examples/actions-repository-permissions.yaml'
x-github:
  enabledForGitHubApps: true
  githubCloudOnly: false
  category: actions
  subcategory: permissions
x-github-releases:
  - ghec
  - api.github.com
  - ghes:
      - '>= 2.23'
x-github-internal:
  path: '/repos/{owner}/{repo}/actions/permissions'
  http-method: get
  service-tier: 1
  published: true
  owner: '@github/c2c-actions'
x-githubEnterpriseOverlays:
  '*':
    description: |-
      Gets the GitHub Actions permissions policy for a repository, including whether GitHub Actions is enabled and the actions allowed to run in the repository.

      You must authenticate using an access token with the `repo` scope to use this endpoint. GitHub Apps must have the `administration` repository permission to use this API.
