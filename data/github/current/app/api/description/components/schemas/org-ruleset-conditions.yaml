---
title: Organization ruleset conditions
type: object
description: >
  Conditions for an organization ruleset.
  The conditions object should contain both `repository_name` and `ref_name` properties or both `repository_id` and `ref_name` properties.
oneOf:
  - type: object
    title: repository_name_and_ref_name
    description: Conditions to target repositories by name and refs by name
    allOf:
    - $ref: 'repository-ruleset-conditions.yaml'
    - $ref: 'repository-ruleset-conditions-repository-name-target.yaml'
  - type: object
    title: repository_id_and_ref_name
    description: Conditions to target repositories by id and refs by name
    allOf:
    - $ref: 'repository-ruleset-conditions.yaml'
    - $ref: 'repository-ruleset-conditions-repository-id-target.yaml'
  - type: object
    title: repository_property_and_ref_name
    description: Conditions to target repositories by property and refs by name
    allOf:
    - $ref: 'repository-ruleset-conditions.yaml'
    - $ref: 'repository-ruleset-conditions-repository-property-target.yaml'
x-githubEnterpriseOverlays:
  '< 3.13':
    oneOf:
    - type: object
      title: repository_name_and_ref_name
      description: Conditions to target repositories by name and refs by name
      allOf:
      - $ref: 'repository-ruleset-conditions.yaml'
      - $ref: 'repository-ruleset-conditions-repository-name-target.yaml'
    - type: object
      title: repository_id_and_ref_name
      description: Conditions to target repositories by id and refs by name
      allOf:
      - $ref: 'repository-ruleset-conditions.yaml'
      - $ref: 'repository-ruleset-conditions-repository-id-target.yaml'
