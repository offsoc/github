<% page_info title: "Security · Secret Scanning · #{this_business}" %>

<%= render(ConditionalAccess::UnauthorizedAccountsComponent.new(
  cap_filter: cap_filter,
  classes: "Box mb-3",
  accounts: unauthorized_orgs,
  resource_label: "alerts"
)) %>

<%= render(SecurityCenter::SubheadComponent.new(
  actor: current_user,
  scope: this_business,
  heading: "Secret scanning alerts",
)) do |c|
  c.with_phase_label
  c.with_feedback_link
end %>

<%= main_section %>
