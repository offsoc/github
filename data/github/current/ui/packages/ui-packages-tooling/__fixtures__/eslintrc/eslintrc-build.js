// Note that this directory isn't a fixure, but we put it here so
// that our fixtures' eslintrc files can reference it as they would
// in a real project.
module.exports = {
  extends: '../../../eslintrc/eslintrc-build.js',
}
