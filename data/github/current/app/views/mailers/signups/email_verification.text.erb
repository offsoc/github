Almost done, @<%= @user %>!

<% if @user.must_verify_email? %>To complete your GitHub sign up, <% else %>To secure your GitHub account, <% end %>we just need to verify your email address: <%= @email %>.

Click the link below to verify your email address:
<%= @cta_url %>

<% if @user.must_verify_email? %>Once verified, you can start using all of GitHub's features to explore, build, and share projects.<% else %>This will let you receive notifications and password resets from GitHub.<% end %>



You're receiving this email because you recently created a new GitHub account or added a new email address. If this wasn’t you, please ignore this email.

---
Sent with <3 by GitHub.
<%= GitHub.physical_address(multiline: true).join("\n") %>
