---
title: milestone created event
type: object
properties:
  action:
    type: string
    enum:
      - created
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  milestone:
    $ref: ../webhooks_milestone_3.yaml
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  repository:
    $ref: '../repository-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - action
  - milestone
  - repository
  - sender
