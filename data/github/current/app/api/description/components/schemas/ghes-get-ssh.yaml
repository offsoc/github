---
type: array
items:
  type: object
  properties:
    key:
      type: string
      format: ssh-key
      description: |-
        The full public key
    fingerprint:
      type: string
      format: ssh-key fingerprint
      description: |-
        The fingerprint of the key
