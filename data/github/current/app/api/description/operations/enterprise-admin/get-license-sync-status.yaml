---
summary: 'Get a license sync status'
description: |-
  Gets information about the status of a license sync job for an enterprise.

  The authenticated user must be an enterprise admin to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
tags:
  - enterprise-admin
operationId: enterprise-admin/get-license-sync-status

externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/license#get-a-license-sync-status'

x-github-resource-owner: '@github/licensing'

x-github-internal:
  path: /enterprises/{enterprise}/license-sync-status
  http-method: get
  published: true
  owner: '@github/licensing'

x-github-releases:
  - ghec

parameters:
  - $ref: '../../components/parameters/enterprise.yaml'

responses:
  '200':
    description: License Sync Status Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/get-license-sync-status.yaml'
        examples:
          default:
            $ref: '../../components/examples/get-license-sync-status.yaml'

x-github:
  githubCloudOnly: true
  enabledForGitHubApps: false
  previews: []
  category: enterprise-admin
  subcategory: license
