# typed: true
# frozen_string_literal: true

module Planning
  module MemexProjectColumn
    module <%= class_name %>Helper
      extend T::Sig
      extend ::FactoryBot::Syntax::Methods

      sig { returns(IndexRepairTestCase) }
      def self.setup_index_repair_test
        item = create(:memex_project_item)
        column = # TODO: return Memex column associated with the field
        expected_value = "TODO"
        IndexRepairTestCase.new(item: item, field: column.to_field, expected_value: expected_value)
      end
    end
  end
end
