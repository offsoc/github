import type {Meta} from '@storybook/react'

import {ReactionViewerLoading} from './ReactionViewerLoading'

const meta = {
  title: 'ReactionViewer/ReactionViewerLoading',
  component: ReactionViewerLoading,
} satisfies Meta<typeof ReactionViewerLoading>

export default meta

export const Example = {
  args: {},
}
