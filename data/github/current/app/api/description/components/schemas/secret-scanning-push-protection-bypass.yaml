---
type: object
properties:
  reason:
    $ref: './secret-scanning-push-protection-bypass-reason.yaml'
  expire_at:
    type: string
    format: date-time
    description: 'The time that the bypass will expire in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.'
    nullable: true
  token_type:
    type: string
    description: 'The token type this bypass is for.'
