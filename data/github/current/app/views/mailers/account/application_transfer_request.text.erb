Hello there,

@<%= @requester.display_login %> wants to transfer the following OAuth Application:

<%= @application.name %>

to your <%= @target.organization? ? "organization" : "account" %> <%= @target.safe_profile_name %>.

You can review and accept the transfer at the following link:

<% if @target.organization? %>
<%= settings_org_application_transfer_url(@target, @xfer) %>
<% else %>
<%= settings_user_application_transfer_url(@target, @xfer) %>
<% end %>

Questions? Problems? <%= GitHub.support_link_text %> any time.

Thanks,
Your friends at GitHub
