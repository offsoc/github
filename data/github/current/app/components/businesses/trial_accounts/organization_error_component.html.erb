<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%= render(Primer::Beta::Flash.new(
  scheme: :danger,
  icon: :alert,
  classes: "js-enterprise-trial-org-invite-warning",
  test_selector: test_selector,
  mb: 3
)) do %>
  The <%= link_to organization.display_login, user_path(organization), class: "Link--inTextBlock" %> organization cannot be invited
  because it <%= error_reason %>. Please
  <%= any_organizations? ? "select a different organization" : "continue without an organization" %>
  or <%= link_to "contact our sales team", ent_contact_url, class: "Link--inTextBlock" %> for assistance.
<% end %>
