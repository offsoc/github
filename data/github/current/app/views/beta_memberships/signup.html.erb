<%# view is an instance of BetaMemberships::View %>

<% content_for :head do %>
  <%= stylesheet_bundle :site %>
<% end %>

<% page_info title: "Join the #{beta.feature_name} waitlist" %>

<%= render Beta::SignupComponent.new(beta: beta)
%>
