#!/usr/bin/env ruby

require "bundler/setup"
require "hydro"

Dir[File.expand_path("../../test/fixtures/generated/*.rb", __FILE__)].each { |f| require f }

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start

require "irb"
IRB.start(__FILE__)
