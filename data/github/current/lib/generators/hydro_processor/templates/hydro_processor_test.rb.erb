# frozen_string_literal: true

require "test_helper"

module GitHub::StreamProcessors
  class <%= class_name %>Test < GitHub::TestCase
    include HydroTestHelpers

    test "it works" do
      # Fill in the details of an example Hydro message here
      message = {}

      hydro_publisher.publish(message, schema: "<%= options[:topic] %>")

      run_processor(GitHub::StreamProcessors::<%= class_name %>.new)

      # Make assertions
    end
  end
end
