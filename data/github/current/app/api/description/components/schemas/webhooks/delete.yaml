---
title: delete event
type: object
properties:
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  pusher_type:
    $ref: ../webhooks_deploy_pusher_type.yaml
  ref:
    $ref: ../webhooks_ref_0.yaml
  ref_type:
    description: The type of Git ref object deleted in the repository.
    type: string
    enum:
      - tag
      - branch
  repository:
    $ref: '../repository-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - ref
  - ref_type
  - pusher_type
  - repository
  - sender
