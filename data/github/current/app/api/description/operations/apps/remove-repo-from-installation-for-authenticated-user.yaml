---
summary: Remove a repository from an app installation
description: |-
    Remove a single repository from an installation. The authenticated user must have admin access to the repository. The installation must have the `repository_selection` of `selected`. 
    
    This endpoint only works for PATs (classic) with the `repo` scope.
tags:
  - apps
operationId: apps/remove-repo-from-installation-for-authenticated-user
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/apps/installations#remove-a-repository-from-an-app-installation'
parameters:
  - $ref: '../../components/parameters/installation-id.yaml'
  - $ref: '../../components/parameters/repository-id.yaml'
responses:
  '204':
    description: Response
  '403':
    $ref: '../../components/responses/forbidden.yaml'
  '304':
    $ref: '../../components/responses/not_modified.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
  '422':
    description: Returned when the application is installed on `all` repositories in the organization, or if this request would remove the last repository that the application has access to in the organization.
x-github-releases:
  - ghec
  - ghes: '>= 2.18'
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: apps
  subcategory: installations
x-githubEnterpriseOverlays:
  '<3.10':
    description: |-
        Remove a single repository from an installation. The authenticated user must have admin access to the repository.

        You must use a personal access token (which you can create via the [command line](${externalDocsUrl}/github/authenticating-to-github/creating-a-personal-access-token) or [Basic Authentication](${externalDocsUrl}/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication)) to access this endpoint.
    responses:
      '422':
x-github-internal:
  path: '/user/installations/{installation_id}/repositories/{repository_id}'
  http-method: delete
  service-tier: 2
  owner: '@github/ecosystem-apps'
  published: true
