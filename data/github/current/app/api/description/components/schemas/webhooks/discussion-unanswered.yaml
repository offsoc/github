---
title: discussion unanswered event
type: object
properties:
  action:
    type: string
    enum:
      - unanswered
  discussion:
    $ref: '../discussion.yaml'
  old_answer:
    $ref: ../webhooks_answer.yaml
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  repository:
    $ref: '../repository-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - action
  - discussion
  - old_answer
  - repository
