---
summary: Get public key for the authenticated user
description: |-
  Gets your public key, which you need to encrypt secrets. You need to encrypt a secret before you can create or update secrets.

  The authenticated user must have Codespaces access to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
tags:
  - codespaces
operationId: codespaces/get-public-key-for-authenticated-user
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/codespaces/secrets#get-public-key-for-the-authenticated-user'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/codespaces-user-public-key.yaml'
        examples:
          default:
            $ref: '../../components/examples/codespaces-user-public-key.yaml'
x-github-internal:
  path: '/user/codespaces/secrets/public-key'
  http-method: get
  published: true
  owner: '@github/codespaces-team'
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: codespaces
  subcategory: secrets
