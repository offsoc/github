---
summary: Get latest Pages build
description: |-
  Gets information about the single most recent build of a ${apiName} Pages site.

  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
tags:
  - repos
operationId: repos/get-latest-pages-build
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/pages/pages#get-latest-pages-build'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/page-build.yaml'
        examples:
          default:
            $ref: '../../components/examples/page-build.yaml'
x-github-releases:
  - ghec
  - ghes: '>= 2.18'
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: pages
  subcategory: pages
x-github-internal:
  path: '/repos/{owner}/{repo}/pages/builds/latest'
  http-method: get
  service-tier: 1
  owner: '@github/pages'
  published: true
