---
summary: Deprecated - List custom repository roles in an organization
description: |-
  > [!WARNING]
  > **Deprecation notice:** This operation is deprecated and will be removed in the future. Use the "[List custom repository roles](${externalDocsUrl}/rest/orgs/custom-roles#list-custom-repository-roles-in-an-organization)" endpoint instead.

  List the custom repository roles available in this organization. For more information on custom repository roles, see "[About custom repository roles](${externalDocsUrl}/organizations/managing-peoples-access-to-your-organization-with-roles/about-custom-repository-roles)."

  The authenticated user must be administrator of the organization or of a repository of the organization to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `admin:org` or `repo` scope to use this endpoint.
tags:
  - orgs
operationId: orgs/list-custom-roles
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/orgs/custom-roles#deprecated---list-custom-repository-roles-in-an-organization'
x-github-internal:
  path: '/organizations/{organization_id}/custom_roles'
  http-method: get
  # need to keep this endpoint published for GHES 3.4 to 3.8
  published: true
  owner: '@github/authorization'
parameters:
  - name: organization_id
    description: The unique identifier of the organization.
    in: path
    required: true
    schema:
      type: string
x-github-releases:
  - ghec
  - ghes: '>= 3.4'
responses:
  '200':
    description: Response - list of custom role names
    content:
      application/json:
        schema:
          type: object
          properties:
            total_count:
              description: The number of custom roles in this organization
              example: 3
              type: integer
            custom_roles:
              type: array
              items:
                $ref: '../../components/schemas/organization-custom-repository-role.yaml'
        examples:
          default:
            $ref: '../../components/examples/organization-custom-repository-role-example.yaml'
x-github:
  githubCloudOnly: true
  enabledForGitHubApps: true
  removalDate: '2025-02-15'
  deprecationDate: '2023-02-15'
  category: orgs
  subcategory: custom-roles
deprecated: true
x-deprecation-details:
  date: '2023-02-15'
  sunsetDate: '2025-02-15'
x-github-breaking-changes:
  - changeset: update_custom_repo_role_path
    patch:
      response:
        '200':
x-githubEnterpriseOverlays:
  # before the 3.9 release, the endpoint should not have the deprecation note
  '< 3.9':
    x-github-internal:
      published: true
    summary: List custom repository roles in an organization
    description: |-
      List the custom repository roles available in this organization. In order to see custom
      repository roles in an organization, the authenticated user must be an organization owner.

      To use this endpoint the authenticated user must be an administrator for the organization or of an repository of the organization and must use an access token with `admin:org repo` scope.
      GitHub Apps must have the `organization_custom_roles:read` organization permission to use this endpoint.

      For more information on custom repository roles, see "[About custom repository roles](${externalDocsUrl}/organizations/managing-peoples-access-to-your-organization-with-roles/about-custom-repository-roles)."
    responses:
      '200':
        content:
          application/json:
            examples:
              default:
                $ref: '../../components/examples/organization-custom-repository-role-example-old.yaml'
