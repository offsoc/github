<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%= render(Primer::Beta::Subhead.new) do |component| %>
  <% component.with_heading(tag: :h2) do %>
    Migration
  <% end %>
<% end %>

<p>The team discussions in this team are currently marked as <strong>migrating</strong>.</p>
<p>If this migration is stuck, you can try enqueuing a new job to complete the conversion.</p>

<% unless GitHub.enterprise? %>
  <p <%= test_selector("migration-splunk-prompt") %>>
    You can also search for migration logs in <a class="Link--inTextBlock" href="<%= splunk_url %>">Splunk</a>.
  </p>
<% end %>

<%= form_with(url: form_path) do %>
  <%= render Primer::ButtonComponent.new(type: :submit).with_content("Enqueue migration job") %>
<% end %>
