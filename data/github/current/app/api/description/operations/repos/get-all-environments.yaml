---
summary: List environments
description: |-
  Lists the environments for a repository.

  Anyone with read access to the repository can use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
tags:
  - repos
operationId: repos/get-all-environments
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/deployments/environments#list-environments'
x-github-internal:
  path: '/repos/{owner}/{repo}/environments'
  http-method: get
  owner: '@github/c2c-actions'
  published: true
x-github-releases:
  - ghec
  - api.github.com
  - ghes: '>= 3.2'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: object
          properties:
            total_count:
              description: The number of environments in this repository
              example: 5
              type: integer
            environments:
              type: array
              items:
                $ref: '../../components/schemas/environment.yaml'
        examples:
          default:
            $ref: '../../components/examples/environments.yaml'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: deployments
  subcategory: environments
