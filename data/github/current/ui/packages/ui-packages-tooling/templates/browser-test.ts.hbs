import {assert, fixture, html, setup, suite, test} from '@github-ui/browser-tests'
import { {{~pascalCase packageName~}} } from '../{{ packageName }}'

suite('{{ packageName }}', () => {
  let container: {{pascalCase packageName}}

  setup(async function () {
    container = await fixture(html`<{{ elementName }}></{{ elementName }}>`)
  })

  test('isConnected', () => {
    assert.isTrue(container.isConnected)
    assert.instanceOf(container, {{pascalCase packageName}})
  })
})
