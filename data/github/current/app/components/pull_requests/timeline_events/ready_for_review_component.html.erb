<%= render(PullRequests::TimelineEvents::TimelineItemComponent.new(issue_event: issue_event, icon: :eye)) do |component| %>
  <% component.with_body do %>
    marked this pull request as ready for review
  <% end %>
<% end %>
