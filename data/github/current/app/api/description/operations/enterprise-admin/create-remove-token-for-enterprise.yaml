---
summary: Create a remove token for an enterprise
description: |-
  Returns a token that you can pass to the `config` script to remove a self-hosted runner from an enterprise. The token expires after one hour.

  Example using remove token:

  To remove your self-hosted runner from an enterprise, replace `TOKEN` with the remove token provided by this
  endpoint.

  ```
  ./config.sh remove --token TOKEN
  ```

  OAuth app tokens and personal access tokens (classic) need the `manage_runners:enterprise` scope to use this endpoint.
operationId: enterprise-admin/create-remove-token-for-enterprise
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/actions/self-hosted-runners#create-a-remove-token-for-an-enterprise'
parameters:
  - $ref: '../../components/parameters/enterprise.yaml'
responses:
  '201':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/authentication-token.yaml'
        examples:
          default:
            $ref: '../../components/examples/authentication-token-2.yaml'
x-github:
  enabledForGitHubApps: false
  githubCloudOnly: true

  category: actions
  subcategory: self-hosted-runners
x-github-releases:
  - ghec
  - ghes:
      - '>= 2.22'
x-github-internal:
  path: '/enterprises/{enterprise}/actions/runners/remove-token'
  http-method: post
  service-tier: 1
  published: true
  owner: '@github/c2c-actions'
x-githubEnterpriseOverlays:
  '<= 3.2':
    description: |-
      Returns a token that you can pass to the `config` script to remove a self-hosted runner from an enterprise. The token expires after one hour.

      Example using remove token:

      To remove your self-hosted runner from an enterprise, replace `TOKEN` with the remove token provided by this
      endpoint.

      ```
      ./config.sh remove --token TOKEN
      ```

      OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
