<%# view is an instance of Settings::Organization::OrgRoleAssignments::IndexView %>
<% page_info responsive: false, :title => "Organization role assigments", selected_link: :org_role_assignments %>

<%= render layout: "orgs/settings/layout" do %>
  <%= render Primer::Beta::Subhead.new do |subhead| %>
    <% subhead.with_heading(tag: :h2) do %>
      Assign teams or people an organization role
    <% end %>
  <% end %>
  <% page_info title: "Organization Role Assignments", selected_link: :org_role_assignments %>

  <%= render OrgRoles::AssignmentListComponent.new(
    organization:,
    assignees:,
    assignment_data:,
    user_count:,
    team_count:,
    query_hash:,
    active_tab:,
    visible_roles:,
    stafftools: false,
  ) %>
<% end %>
