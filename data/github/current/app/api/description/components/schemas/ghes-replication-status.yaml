---
type: object
properties:
  status:
    $ref: './ghes-replication-status-indicator.yaml'
  nodes:
    type: array
    items:
      type: object
      properties:
        hostname:
          type: string
        status:
          $ref: './ghes-replication-status-indicator.yaml'
        services:
          type: array
          items:
            type: object
            properties:
              status:
                $ref: './ghes-replication-status-indicator.yaml'
              name:
                type: string
              details:
                type: string
