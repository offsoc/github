<% event = view.entry.data %>
Created announcement for the <%= event[:owner] %> <%= event[:owner_type] %> with message "<%= event[:message] %>", dismissibility <code><%= event[:dismissibility] %></code>, and expiration date <code><%= event[:expiry] || "none" %></code>
