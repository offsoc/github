<%# erblint:counter DeprecatedComponentsCounter 1 %>
<% release_dialog_id = "delete-release-#{SecureRandom.hex(4)}-dialog" %>
<%= render Primer::IconButton.new(
  icon: :trash,
  scheme: :danger,
  test_selector: "delete-button",
  "aria-label": "Delete: #{@release.display_name}",
  id: "dialog-show-#{release_dialog_id}",
  "data-show-dialog-id": release_dialog_id,
  **system_arguments)
%>
<%= render Releases::DeleteConfirmationComponent.new(dialog_id: release_dialog_id, release: release) %>
