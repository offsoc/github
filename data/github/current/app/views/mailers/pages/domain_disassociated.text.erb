<% len = @repositories.length %>

Domain <%= @domain_name %> is no longer verified. As a result, the custom domain for the following GitHub Pages <%= "site".pluralize(len) %> has been removed:

<%= @repositories.join("\n") %>

The GitHub Pages <%= "site".pluralize(len) %> <%= "is".pluralize(len) %> still accessible on the default github.io domain.

Manage your verified domains at <%= @pages_settings_url %>.
