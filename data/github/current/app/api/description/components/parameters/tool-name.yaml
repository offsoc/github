---
name: tool_name
description: The name of a code scanning tool. Only results by this tool will be listed.
  You can specify the tool by using either `tool_name` or `tool_guid`, but not both.
in: query
required: false
schema:
  $ref: '../../components/schemas/code-scanning-analysis-tool-name.yaml'
