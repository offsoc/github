<%
  breadcrumb_object = if page_breadcrumb_object.present?
    page_breadcrumb_object
  elsif defined?(current_repository) && current_repository.present?
    current_repository
  elsif defined?(this_user) && this_user.present?
    this_user
  end
%>
<%= render Site::BreadcrumbHeaderComponent.new(
  object: breadcrumb_object,
  title: breadcrumb_title,
  owner: page_breadcrumb_owner
) %>
