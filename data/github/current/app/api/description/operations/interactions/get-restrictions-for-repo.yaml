---
summary: Get interaction restrictions for a repository
description: Shows which type of GitHub user can interact with this repository and
  when the restriction expires. If there are no restrictions, you will see an empty
  response.
tags:
  - interactions
operationId: interactions/get-restrictions-for-repo
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/interactions/repos#get-interaction-restrictions-for-a-repository'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          anyOf:
            - $ref: '../../components/schemas/interaction-limit-response.yaml'
            - type: object
              properties: {}
              additionalProperties: false
        examples:
          default:
            $ref: '../../components/examples/interaction-limit-2.yaml'
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true

  category: interactions
  subcategory: repos
x-github-internal:
  path: '/repos/{owner}/{repo}/interaction-limits'
  http-method: get
  service-tier: 2
  owner: '@github/trust-safety-engineering'
  published: true
