<%= render(@item) do |item| %>
  <% available_sections.each do |section| %>
    <% item.with_item(label: get_display_name(section), href: get_path(section), selected_by_ids: get_item_ids(section)) %>
  <% end %>
  <% item.with_leading_visual_icon(icon: :codespaces) %>
<% end %>
