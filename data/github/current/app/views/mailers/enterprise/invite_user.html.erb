<% content_for :header do %>
  Someone has created an account for you on a GitHub Enterprise Server instance.
<% end %>

<%= render(Mail::HeaderTitleComponent.new(title: "GitHub Enterprise Server", icon: "enterprise.png")) %>

<%= render(Mail::SpacerComponent.new) %>

<p>
  Your username on the instance is <strong><%= @user.display_login %></strong>.<br>
  Your email on the instance is <strong><%= @user.email %></strong>.<br>
  Click below to set your password.
</p>

<%= render(Mail::SpacerComponent.new) %>

<%= render(Mail::ContainerComponent.new) do %>
  <%= render(
    Mail::ButtonComponent.new(
      text: "Set your password",
      url: @reset_link,
      classes: "btn-primary btn-large"
    )
  ) %>
<% end %>

<%= render(Mail::SpacerComponent.new) %>

<p>
  This link will expire in <%= pluralize(@hours_until_expiry, "hour") %>.<br>
  To get a new password reset link, go to
  <%= link_to "#{@url}/password_reset", "#{@url}/password_reset" %>.
</p>

<%= render(Mail::SpacerComponent.new) %>

<p>
  After setting your password, sign in at
  <%= link_to "#{@url}/login", "#{@url}/login" %>
  to begin using your instance.
</p>

<% content_for :footer_text do %>
  You're receiving this email because someone created an account for you on a GitHub Enterprise Server instance.
<% end %>
