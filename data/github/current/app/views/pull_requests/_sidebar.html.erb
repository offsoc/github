<%# rubocop:disable Lint/InstanceVariable %>
<% pull_node = defined?(pull_node) ? pull_node : nil %>
<% deferred_content = defined?(deferred_content) ? deferred_content : false %>

<%= render "issues/sidebar", deferred_content: deferred_content, issue: pull.issue, issue_node: pull_node, discussion_categories: [] %>
