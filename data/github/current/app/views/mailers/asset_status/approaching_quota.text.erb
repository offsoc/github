We wanted to let you know that you’ve used 80% of your data plan for Git LFS on <%= @user.organization? ? "the organization" : "your personal account" %> <%= @user %>. No immediate action is necessary, but you might want to consider purchasing additional data packs to cover your bandwidth and storage usage:

  <%= @user.organization? ? "#{GitHub.url}/organizations/#{@user}/billing/data/upgrade" : "#{GitHub.url}/account/billing/data/upgrade" %>

Current usage as of <%= @asset_status.updated_at.strftime("%d %b %Y %I:%M%p %Z") %>:

  Bandwidth: <%= @asset_status.bandwidth_usage.round(2) %> GB / <%= @asset_status.bandwidth_quota.round %> GB (<%= @asset_status.bandwidth_usage_percentage %>%)
  Storage: <%= @asset_status.storage_usage.round(2) %> GB / <%= @asset_status.storage_quota.round %> GB (<%= @asset_status.storage_usage_percentage %>%)
