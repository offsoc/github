---
summary: 'List enterprise teams'
description: List all teams in the enterprise for the authenticated user
tags:
  - enterprise-teams
operationId: enterprise-teams/list

externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-teams/enterprise-teams#list-enterprise-teams'

x-github-internal:
  path: /enterprises/{enterprise}/teams
  http-method: get
  published: false
  owner: '@github/identity-tnt'

x-github-releases:
  - api.github.com
  - ghec

parameters:
 - $ref: '../../components/parameters/enterprise.yaml'
 - $ref: '../../components/parameters/per-page.yaml'
 - $ref: '../../components/parameters/page.yaml'

responses:
 '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/enterprise-team.yaml'
        examples:
          default:
            $ref: '../../components/examples/enterprise-teams-items.yaml'
    headers:
      Link:
        $ref: '../../components/headers/link.yaml'
 '403':
    $ref: '../../components/responses/forbidden.yaml'

x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-teams
  subcategory: enterprise-teams
