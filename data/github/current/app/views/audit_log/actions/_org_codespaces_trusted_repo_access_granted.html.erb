<% if view.entry.repo.present? %>
  Added <span class="context"><%= view.link_to_repo %></span> to Codespaces trusted repo access based on existing organization setting "All repositories"
<% else %>
  Granted Codespaces trusted repo access to all repositories on the <span class="context"><%= view.entry.safe_organization_name %></span> organization
<% end %>
