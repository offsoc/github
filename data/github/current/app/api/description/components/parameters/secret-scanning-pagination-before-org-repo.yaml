---
name: before
description: 'A cursor, as given in the [Link header](${externalDocsUrl}/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events before this cursor. To receive an initial cursor on your first request, include an empty "before" query string.'
in: query
required: false
schema:
  type: string
