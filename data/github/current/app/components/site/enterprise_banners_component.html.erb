<% if !content_for?(:enterprise_announcement_preview) %>
  <% GitHub.dogstats.time("repos.enterprise_announcements.render_time") do %>
    <% banners.each do |banner| %>
      <%= render partial: "settings/announcements/enterprise_announcement", locals: { banner: banner }%>
    <% end %>
  <% end %>
<% end %>
