<% if !plan.free? && target.payment_difference(plan) != 0 %>
  Your <%= target.plan_duration %>ly bill will
  <%= increase_or_decrease(target, plan) %>
  from <strong><%= casual_currency target.payment_amount  %></strong>
  to <strong><%= casual_currency target.payment_amount(:plan => plan) %></strong>.

  <% if target.payment_difference(plan) > 0 %>
    <% if target.yearly_plan? %>
      You’ll be charged a prorated amount of <strong><%= casual_currency target.payment_difference(plan) %></strong>
      today for this upgrade.
    <% end %>
  <% end %>
<% end %>
