import type { {{~routeComponent}}Payload} from '../routes/{{routeComponent}}'

export function get{{routeComponent}}RoutePayload(): {{routeComponent}}Payload {
  return {
    someField: 'Payload for the {{packageName}} {{routeComponent}} route',
  }
}
