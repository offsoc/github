---
name: until
description: 'Only show rule suites updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.'
in: query
schema:
  type: string
  format: date-time
