<% if show_org_admin_text %>
  <%= render(Primer::Beta::Text.new(font_weight: :bold)) { billable_owner.display_login } %> has a billing issue. Please adjust <%= render(Primer::Beta::Text.new(font_weight: :bold)) { billable_owner.display_login } %>'s billing settings to continue using codespaces.
<% elsif show_org_user_text %>
  <%= render(Primer::Beta::Text.new(font_weight: :bold)) { billable_owner.display_login } %> has a billing issue. Please contact an administrator to continue using codespaces.
<% else %>
  You seem to have a billing issue. Please adjust your billing settings to continue using codespaces.
<% end %>
