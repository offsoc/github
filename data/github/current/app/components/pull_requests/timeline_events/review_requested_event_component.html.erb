<%= render(PullRequests::TimelineEvents::TimelineItemComponent.new(issue_event: issue_events.first, icon: "eye")) do |component| %>
  <% component.with_body do %>
    <%= render description_component %>
  <% end %>
<% end %>
