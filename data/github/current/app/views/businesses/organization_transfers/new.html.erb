<% page_info \
  title: this_business.name,
  selected_link: :business_member_organizations,
  stafftools: stafftools_enterprise_path(this_business) %>

<%= render layout: "businesses/layout", locals: {
    business: this_business,
    title: "Transfer organization",
    back_to_link_path: enterprise_organizations_path(this_business),
    back_to_link_text: "organizations",
  } do %>
  <%= render(Businesses::OrganizationTransfers::FormComponent.new(
    transfer: transfer,
    organization: transfer.organization,
    from_business: transfer.from_business)) %>
<% end %>
