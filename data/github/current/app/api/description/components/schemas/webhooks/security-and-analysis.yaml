---
title: security_and_analysis event
type: object
properties:
  changes:
    type: object
    properties:
      from:
        type: object
        properties:
          security_and_analysis:
            $ref: '../security-and-analysis.yaml'
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  repository:
    $ref: '../full-repository.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - changes
  - repository
