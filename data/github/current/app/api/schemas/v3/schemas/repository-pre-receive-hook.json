{
  "$schema": "http://json-schema.org/draft-04/hyper-schema",
  "id": "https://schema.github.com/v3/repository-pre-receive-hook.json",
  "description": "A pre-receive hook that is enabled or testing on a repository",
  "title": "Repository pre-receive hook",
  "type": "object",
  "definitions": {
    "id": {
      "description": "Unique identifier of the pre-receive hook",
      "example": 42,
      "type": "integer"
    },
    "name": {
      "description": "The name of the pre-receive hook",
      "example": "Commit message enforcement",
      "type": "string"
    },
    "enforcement": {
      "description": "The state of enforcement for the pre-receive hook on this repository",
      "type": "string",
      "enum": [
        "enabled",
        "disabled",
        "testing"
      ]
    },
    "configuration_url": {
      "description": "The configuration URL for the pre-receive hook on the repository",
      "type": "string",
      "format": "uri"
    }
  },
  "properties": {
    "id": {
      "type": "integer"
    },
    "name": {
      "type": "string"
    },
    "enforcement": {
      "type": "string"
    },
    "configuration_url": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "id",
    "name",
    "enforcement",
    "configuration_url"
  ]
}
