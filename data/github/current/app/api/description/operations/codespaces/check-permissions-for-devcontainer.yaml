---
summary: Check if permissions defined by a devcontainer have been accepted by the authenticated user
description: |-
  Checks whether the permissions defined by a given devcontainer configuration have been accepted by the authenticated user.

  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
tags:
  - codespaces
operationId: codespaces/check-permissions-for-devcontainer
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/codespaces/codespaces#check-if-permissions-defined-by-a-devcontainer-have-been-accepted-by-the-authenticated-user'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - name: ref
    description: The git reference that points to the location of the devcontainer configuration to use for the permission check. The value of `ref` will typically be a branch name (`heads/BRANCH_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    in: query
    required: true
    schema:
      type: string
      example: master
  - name: devcontainer_path
    description: Path to the devcontainer.json configuration to use for the permission check.
    in: query
    required: true
    schema:
      type: string
      example: .devcontainer/example/devcontainer.json
responses:
  '200':
    description: Response when the permission check is successful
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/codespaces-permissions-check-for-devcontainer.yaml'
        examples:
          default:
            $ref: '../../components/examples/codespaces-permissions-check-for-devcontainer.yaml'
  '401':
    $ref: '../../components/responses/requires_authentication.yaml'
  '403':
    $ref: '../../components/responses/forbidden.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
  '422':
    $ref: '../../components/responses/validation_failed.yaml'
  '503':
    $ref: '../../components/responses/service_unavailable.yaml'
x-github-internal:
  path: '/repos/{owner}/{repo}/codespaces/permissions_check'
  http-method: get
  published: true
  owner: '@github/codespaces-team'
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: codespaces
  subcategory: codespaces
