<% if view.org? %>
  Organization projects are enabled for the <span class="context"><%= view.link_to_org %></span> organization.
<% else %>
  Organization projects are now enabled for all organizations in the <span class="context"><%= view.link_to_business %></span> enterprise.
<% end %>
