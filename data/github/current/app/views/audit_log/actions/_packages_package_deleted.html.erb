<% if view.version_count && view.version_count > 0 && view.storage_bytes && view.storage_bytes > 0 %>
  Deleted <%= view.ecosystem %> package <%= view.package %> containing <%= view.version_count %> <%= view.version_count > 1 ? "versions" : "version" %> removing <%= view.storage_bytes(human_readable: true) %> from the <%= view.org_name %> organization
<% else %>
  Deleted <%= view.ecosystem %> package <%= view.package %> from the <%= view.org_name %> organization
<% end %>
