---
title: Secret Scanning Alert Location Created Event
type: object
properties:
  action:
    type: string
    enum:
      - created
  alert:
    $ref: '../secret-scanning-alert-webhook.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  location:
    $ref: '../secret-scanning-location.yaml'
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  repository:
    $ref: '../repository-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - location
  - alert
  - repository
  - sender
