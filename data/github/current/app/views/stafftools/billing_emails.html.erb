<% page_info title: "Billing emails · #{owner.safe_profile_name}" %>

<%= render Primer::Beta::Subhead.new do |component| %>
  <% component.with_heading(tag: :h2) do %>Billing emails<% end %>
<% end %>

<%=
  render Stafftools::Billing::BillingEmailRecipientsListComponent.new(
    primary_email: owner.billing_email,
    external_emails: owner.billing_external_emails.to_a
  )
%>
