# This is a pseudo-package that checks to ensure library code doesn't incorrectly depend on app code.
#
# IMPORTANT: This package should NEVER have dependencies added to it. All dependency violations for this
# package must be resolved through other means.
enforce_dependencies: true
enforce_privacy: false
