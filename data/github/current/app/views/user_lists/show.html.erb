<%
  page_info(
    class: class_names("page-profile", "mine" => list.owned_by?(current_user)),
    stafftools: stafftools_user_path(list.user_login),
    container_xl: true,
    title: list.title_for(current_user),
    richweb: {
      title: list.title_for(current_user),
      description: list.description,
      updated_time: list.updated_at,
      image: list.og_image_url,
    }
  )
%>

<%= render(Profiles::User::LayoutComponent.new(layout_data: layout_data)) do %>
  <%= render(UserLists::ShowComponent.new(list: list, page: current_page)) %>
<% end %>
