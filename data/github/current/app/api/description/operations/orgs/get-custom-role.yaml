---
summary: Deprecated - Get a custom role
description: |-
  > [!WARNING]
  > **Deprecation notice:** This operation is deprecated and will be removed after September 6, 2023. Use the "[Get a custom repository role](${externalDocsUrl}/rest/orgs/custom-roles#get-a-custom-repository-role)" endpoint instead.

  Gets a custom repository role that is available to all repositories owned by the organization. For more information on custom repository roles, see "[About custom repository roles](${externalDocsUrl}/organizations/managing-peoples-access-to-your-organization-with-roles/about-custom-repository-roles)."

  The authenticated user must be an administrator of the organization or of a repository of the organization to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `admin:org` or `repo` scope to use this endpoint.
tags:
  - orgs
operationId: orgs/get-custom-role

externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/orgs/custom-roles#deprecated---get-a-custom-role'

x-github-internal:
  path: /orgs/{org}/custom_roles/{role_id}
  http-method: get
  published: true
  owner: '@github/authorization'

x-github-releases:
  - ghec

parameters:
  - $ref: '../../components/parameters/org.yaml'
  - $ref: '../../components/parameters/role-id.yaml'

responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/organization-custom-repository-role.yaml'
        examples:
          default:
            $ref: '../../components/examples/organization-custom-repository-role-example.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'

x-github:
  githubCloudOnly: true
  enabledForGitHubApps: true
  removalDate: '2022-09-06'
  deprecationDate: '2023-03-06'
  category: orgs
  subcategory: custom-roles
deprecated: true
x-deprecation-details:
  date: '2023-03-06'
  sunsetDate: '2023-09-06'
