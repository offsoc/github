<%= render Issues::LabelComponent.new(
    color: label.color,
    name: label.name,
    description: label.description,
    href: href,
    classes: "Label--inline") do |component| %>
  <%= label.name_html -%>
<% end %>
