<div class="pane">
  <h2>
    No Active Components
  </h2>
  <p>Oops! There's nothing to see here because no dashboard components are activated.</p>
  <p>Learn more in the docs:</p>
  <ul>
    <% components.select { |c| c.respond_to?(:doc_url) }.each do |component| %>
      <li>
        <p><a href="<%= component.doc_url %>"><code><%= component.name %></code></a></p>
      </li>
    <% end %>
  </ul>
</div>
