---
summary: Update a repository invitation
description: ''
tags:
  - repos
operationId: repos/update-invitation
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/collaborators/invitations#update-a-repository-invitation'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - $ref: '../../components/parameters/invitation-id.yaml'
requestBody:
  required: false
  content:
    application/json:
      schema:
        type: object
        properties:
          permissions:
            type: string
            description: The permissions that the associated user will have on the
              repository. Valid values are `read`, `write`, `maintain`, `triage`,
              and `admin`.
            enum:
              - read
              - write
              - maintain
              - triage
              - admin
            x-graceful-enum: true
      examples:
        default:
          summary: Example request body
          value:
            permissions: write
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/repository-invitation.yaml'
        examples:
          default:
            $ref: '../../components/examples/repository-invitation.yaml'
x-github-releases:
  - ghec
  - ghes: '>= 2.18'
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: collaborators
  subcategory: invitations
x-github-internal:
  path: '/repos/{owner}/{repo}/invitations/{invitation_id}'
  http-method: patch
  service-tier: 1
  owner: '@github/trust-safety-engineering'
  published: true
