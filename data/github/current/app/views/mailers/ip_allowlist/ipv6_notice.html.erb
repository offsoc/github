<% content_for :header do %>
  Update the <%= @owner_name_and_type %>’s IP allow list with IPv6 addresses
<% end %>

<%= render(Mail::RowComponent.new) do %>
  <%= render(Mail::ColumnComponent.new) do %>
    GitHub is gradually rolling out IPv6.
  <% end %>
<% end %>

<%= render(Mail::SpacerComponent.new) %>

<%= render(Mail::RowComponent.new) do %>
  <%= render(Mail::ColumnComponent.new) do %>
    As GitHub services continue to add IPv6 support, we will start recognizing
    IPv6 addresses of GitHub users.
  <% end %>
<% end %>

<%= render(Mail::SpacerComponent.new) %>

<%= render(Mail::RowComponent.new) do %>
  <%= render(Mail::ColumnComponent.new) do %>
    To prevent possible access interruptions with the
    <strong><%= @owner_name_and_type %></strong>,
    please ensure you have added any necessary IPv6 addresses to your IP allow list.
  <% end %>
<% end %>

<%= render(Mail::SpacerComponent.new) %>

<%= render(Mail::RowComponent.new) do %>
  <%= render(Mail::ColumnComponent.new) do %>
    If you do not update your IP allow list with all necessary IPv6 addresses,
    users may experience issues accessing and using the
    <strong><%= @owner_name_and_type %></strong>
    as IPv6 support across GitHub services increases.
  <% end %>
<% end %>

<%= render(Mail::SpacerComponent.new) %>

<%= render(
  Mail::ButtonComponent.new(
    text: "Manage the #{@owner_name_and_type} IP allow list",
    url: @ip_allowlist_url,
    classes: "btn-primary btn-large"
  )
) %>

<% content_for :footer_text do %>
  You’re receiving this email because you manage the
  <strong><%= @owner_name_and_type %></strong>,
  which has an IP allow list set up.
<% end %>
