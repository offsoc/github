<%# internal api key from `secrets.conf` needed by enterprise-manage to use GHESManageClient %>
<% internal_api_key = secret_value("ghes-manage", "internal-api-key") %>
<%= Digest::SHA256.hexdigest(internal_api_key) %>
