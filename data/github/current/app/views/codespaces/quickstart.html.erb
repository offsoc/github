<% action = quickstart_codespace.persisted? && quickstart_codespace.provisioned? ? "Resume" : "Create" %>
<% page_info title: "#{action} codespace", stafftools: stafftools_user_codespaces_path(current_user) %>
<%= render(Codespaces::QuickStartComponent.new(quickstart_codespace:, auto_init:)) %>
