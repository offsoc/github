<% if events.length == 1 %>
  added this to <%= render "issues/events/memex_project_link", event: events.first %>
<% else %>
  added this to

  <%= to_sentence(events.map { |event| render("issues/events/memex_project_link", event: event) }, last_word_connector: " and ") %>
<% end %>
