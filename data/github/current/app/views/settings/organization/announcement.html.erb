<% page_info \
  container_xl: true,
  title: "Announcement",
  selected_link: :org_announcements %>

<%= render layout: "orgs/settings/layout" do %>
  <%= render partial: "settings/announcements/announcement_form", locals: {
      banner: banner,
      scope: "organization",
      set_announcement_path: org_set_announcement_path,
      preview_announcement_path: org_announcement_preview_path,
      destroy_announcement_path: org_destroy_announcement_path,
      scope_discussions_path: (org_discussions_path(current_organization) if current_organization.discussion_repository)
    }
  %>
<% end %>
