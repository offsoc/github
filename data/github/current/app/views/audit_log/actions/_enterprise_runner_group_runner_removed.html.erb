<% from_api = view.entry.data["request_category"] == "api" %>
Removed runner <%= view.entry.hit.data[:runner_id] %> from runner group <%= view.entry.hit.data[:runner_group_id] %> for <span class="context"><%= view.link_to_business %></span> <% if from_api %> via the API <% end %>
