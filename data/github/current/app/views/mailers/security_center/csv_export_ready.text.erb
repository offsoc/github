<%= @title %>

Hello <%= @user.display_login %>,

Your <%= @feature %> data is ready for download. It will be available until <%= (Time.now + 3.days).utc.strftime("%B %-d, %Y at %H:%M") %> UTC, and it can be found at <%= GitHub.url %><%= @csv_url %>.

<%= @footer_text %>

Thanks,
GitHub.com
