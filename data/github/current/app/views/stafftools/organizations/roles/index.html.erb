<% page_info title: "Organization roles", selected_link: :org_role_details %>

<%= render Primer::Beta::Subhead.new do |component| %>
  <% component.with_heading(tag: :h2) do %>Organization roles<% end %>
  <% component.with_description do %>
    Listed below are all the available organization roles that can be granted to members and teams in this organization. Expand a role to view the details of the permissions included.
  <% end %>
<% end %>

<%= render OrgRoles::SystemRolesComponent.new(organization: organization, mb: 3) %>
<%= render OrgRoles::CustomRolesListComponent.new(organization: organization, viewer_permissions: { stafftools: true }) %>
