---
summary: Set announcement banner for enterprise
description: Sets the announcement banner to display for the enterprise.
tags:
  - enterprise-admin
operationId: announcement-banners/set-announcement-banner-for-enterprise
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/announcement-banners/enterprises#set-announcement-banner-for-enterprise'
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/announcement.yaml'
      examples:
        default:
          $ref: '../../components/examples/announcement.yaml'

parameters:
  - $ref: '../../components/parameters/enterprise.yaml'

responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/announcement-banner.yaml'
        examples:
          default:
            $ref: '../../components/examples/announcement.yaml'

x-github-releases:
  - ghec

x-github:
  githubCloudOnly: true
  enabledForGitHubApps: false
  category: announcement-banners
  subcategory: enterprises

x-github-internal:
  path: /enterprises/{enterprise}/announcement
  http-method: patch
  owner: '@github/repos'
  published: true
