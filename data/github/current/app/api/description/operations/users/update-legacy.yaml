---
summary: Suspend a user
description: ''
tags:
  - users
operationId: users/update-legacy
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/teams/teams#suspend-a-user'
parameters:
  - $ref: '../../components/parameters/user-id.yaml'
requestBody:
  content:
    application/json:
      schema:
        properties:
          reason:
            description: 'The reason the user is being suspended. If `reason` is not
              provided, the default reason will be used: "Suspended via API by #{site
              administrator that performed the action}."'
            example: Suspended due to inactivity
            type: string
        type: object
        nullable: true
responses:
  '204':
    description: Response
x-github-releases: []
x-github-internal:
  path: '/user/{user_id}/suspended'
  http-method: put
  service-tier: 2
  published: true
