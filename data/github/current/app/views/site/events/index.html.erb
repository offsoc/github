<% page_info(
  class: "f4",
  richweb: {
    title: "GitHub Events",
    url: request.original_url,
    description: "Connect with the GitHub community at virtual conferences, meetups, and hackathons around the world.",
  },
  title: "GitHub Events",
  canonical_url: request.original_url
) %>

<% content_for :head do %>
  <meta name="is_logged_out_page" content="true">
<% end %>

<%= render(Explore::NavComponent.new) %>
<%= render(Explore::Event::IndexListComponent.new(
  hosted_events: ExploreFeed::Event.all.hosted.upcoming_or_current,
  sponsored_events: ExploreFeed::Event.all.sponsored.upcoming_or_current,
)) %>
