<% page_info :title => "Add a Pages verified domain" %>

<%= render layout: "orgs/settings/layout" do %>
  <%= render partial: "settings/pages_protected_domains/show", locals: {
    domain: domain,
    update_url: settings_org_pages_protected_domain_path(current_pages_owner, domain),
    pages_settings_path: settings_org_pages_path(current_pages_owner),
  } %>
<% end %>
