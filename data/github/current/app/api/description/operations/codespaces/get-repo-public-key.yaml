---
summary: Get a repository public key
description: |-
  Gets your public key, which you need to encrypt secrets. You need to
  encrypt a secret before you can create or update secrets.

  Anyone with read access to the repository can use this endpoint.

  If the repository is private, OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
tags:
  - codespaces
operationId: codespaces/get-repo-public-key
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/codespaces/repository-secrets#get-a-repository-public-key'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/codespaces-public-key.yaml'
        examples:
          default:
            $ref: '../../components/examples/codespaces-public-key.yaml'
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: codespaces
  subcategory: repository-secrets
x-github-internal:
  path: '/repos/{owner}/{repo}/codespaces/secrets/public-key'
  http-method: get
  owner: '@github/codespaces-team'
  published: true
