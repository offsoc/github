The state for listing "<%= @listing.name %>" has changed in the GitHub Marketplace.

The event <%= @action %> has changed the state from "<%= @old_state.to_s.humanize.downcase %>" to "<%= @new_state.to_s.humanize.downcase %>".

<%- if @message.present? -%>
-------------------

Our note to the integrator:

<%= @message %>

-------------------

<%- end -%>
View <%= @listing.name %>: <%= marketplace_listing_url(@listing) %>

Manage the listing: <%= biztools_marketplace_listing_url(@listing) %>

Thanks,
GitHub Marketplace Team
