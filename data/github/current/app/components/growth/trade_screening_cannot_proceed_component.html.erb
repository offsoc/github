<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%= render(Primer::Beta::Flash.new(
  icon: trade_screening_error_data[:icon],
  scheme: trade_screening_error_data[:scheme],
  test_selector: "trade-screening-cannot-proceed",
  **system_arguments,
)) do %>
  <%= render(Primer::Beta::Text.new(font_weight: :bold)) { "You can't proceed with your payment" } %>
  <div class="ml-5">
    <%= trade_screening_error_data[:message] %>
  </div>
<% end %>
