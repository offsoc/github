<% if event.status == "" %>
  removed the status
  in <%= render "issues/events/memex_project_link", event: event %>
<% elsif event.previous_status == "" %>
  moved this to <strong><%= event.status %></strong>
  in <%= render "issues/events/memex_project_link", event: event %>
<% else %>
  moved this from <strong><%= event.previous_status %></strong>
  to <strong><%= event.status %></strong>
  in <%= render "issues/events/memex_project_link", event: event %>
<% end %>
