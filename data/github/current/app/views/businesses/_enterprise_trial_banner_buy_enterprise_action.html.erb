<% button_size = business.trial_expired? ? :medium: :large %>

<%= render Primer::Beta::Button.new(
  tag: :a,
  scheme: :primary,
  size: button_size,
  href: billing_settings_upgrade_enterprise_path(business),
  ml: 2
).with_content(buy_enterprise_flavor) %>
