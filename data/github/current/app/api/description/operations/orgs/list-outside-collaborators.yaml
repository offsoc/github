---
summary: List outside collaborators for an organization
description: List all users who are outside collaborators of an organization.
tags:
  - orgs
operationId: orgs/list-outside-collaborators
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/orgs/outside-collaborators#list-outside-collaborators-for-an-organization'
parameters:
  - $ref: '../../components/parameters/org.yaml'
  - name: filter
    description: "Filter the list of outside collaborators. `2fa_disabled` means that only outside collaborators without [two-factor authentication](https://github.com/blog/1614-two-factor-authentication)
      enabled will be returned."
    in: query
    required: false
    schema:
      type: string
      enum:
        - 2fa_disabled
        - all
      default: all
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/simple-user.yaml'
        examples:
          default:
            $ref: '../../components/examples/simple-user-items.yaml'
    headers:
      Link:
        $ref: '../../components/headers/link.yaml'
x-github-releases:
  - ghec
  - ghes: '>= 2.18'
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true

  category: orgs
  subcategory: outside-collaborators
x-github-internal:
  path: '/orgs/{org}/outside_collaborators'
  http-method: get
  service-tier: 2
  owner: '@github/meao'
  published: true
