Hi @<%= @request.requested_by %>! Your request to <%= @request_action %> has been denied by @<%= @request.denied_by %>.

You can send another request via Stafftools to try again.

Reason given for this request:
<%= @request.reason %>

View this <%= @request.accessible_type.downcase %> in stafftools:
<%= @stafftools_url %>

--- Sent with <3 by GitHub.
<%= GitHub.physical_address(multiline: true).join("\n") %>
