<%= render Primer::Beta::Blankslate.new(
  border: true,
  narrow: true,
  test_selector: "org_level_discussions_blankslate",
  "data-hpc": ""
) do |c| %>
  <% c.with_visual_icon(icon: "comment-discussion") %>
  <% c.with_heading(tag: :h2).with_content(title) %>
  <% c.with_description do %>
    Organization discussions are to broadcast news, create conversation in your community, answer questions, and
    share ideas. To get started, you can <%= link_to "create a new discussion.", new_org_discussion_path(this_organization), class: "Link--inTextBlock" %>
  <% end %>
<% end %>
