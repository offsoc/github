---
summary: 'Delete a deployment branch policy'
description: |-
  Deletes a deployment branch or tag policy for an environment.

  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
tags:
  - repos
operationId: repos/delete-deployment-branch-policy
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/deployments/branch-policies#delete-a-deployment-branch-policy'
x-github-internal:
  path: /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}
  http-method: delete
  published: true
  owner: '@github/actions-fusion-reviewers'
x-github-releases:
  - ghec
  - api.github.com
  - ghes: '>= 3.2'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - $ref: '../../components/parameters/environment-name.yaml'
  - $ref: '../../components/parameters/branch-policy-id.yaml'
responses:
  '204':
    description: Response
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: deployments
  subcategory: branch-policies
x-githubEnterpriseOverlays:
  '<3.12':
    description: |-
      Deletes a deployment branch policy for an environment.

      OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
