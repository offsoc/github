---
summary: Get the system requirement check results for configured cluster nodes
description: |-
  Checks if the minimum requirements for system hardware resources are met on each configured cluster node.
  This endpoint may take several seconds to reply.
operationId: enterprise-admin/get-checks-system-requirements
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/manage-ghes#get-the-system-requirement-check-results-for-configured-cluster-nodes'
servers:
  - url: '{protocol}://{hostname}'
    variables:
      hostname:
        default: 'HOSTNAME'
        description: Self-hosted Enterprise Server hostname
      protocol:
        default: 'http'
        description: 'Self-hosted Enterprise Server protocol'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/ghes-checks-system-requirements.yaml'
        examples:
          default:
            $ref: '../../components/examples/ghes-checks-system-requirements.yaml'
  '400':
    description: Bad request
  '500':
    description: Internal error
x-github-releases:
  - ghes: '>= 3.12'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: manage-ghes
x-github-internal:
  path: '/manage/v1/checks/system-requirements'
  http-method: get
  service-tier: 1
  published: true
  owner: "@github/ghes-operator-platform"
