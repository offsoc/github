Transferred the
<span class="context"><%= view.entry.safe_organization_name %></span> organization from the
<span class="context"><%= view.entry.hit[:from_business] %></span> enterprise to the
<span class="context"><%= view.entry.hit[:to_business] %></span> enterprise.
