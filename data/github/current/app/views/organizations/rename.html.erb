<%# erblint:counter DeprecatedComponentsCounter 1 %>
<% page_info title: "Renaming your organization…" %>

<poll-include-fragment src="<%= settings_rename_status_path(organization_id: org.id) %>">
  <%= render Primer::BlankslateComponent.new(
    title: "Renaming your organization…",
    description: "This may take a few minutes to complete.",
    large: true,
    spacious: true,
  ) do |component| %>
    <% component.with_spinner(size: :large) %>
  <% end %>
</poll-include-fragment>
