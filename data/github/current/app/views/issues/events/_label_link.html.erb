<%= render Issues::LabelComponent.new(
    color: label.color,
    name: label.name,
    description: label.description,
    href: label.resource_path,
    display: :inline_block,
    vertical_align: :middle) do |component| %>
  <%= label.name_html %>
<% end %>
