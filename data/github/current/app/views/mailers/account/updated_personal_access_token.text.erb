Previously, the <%= @token_type %> "<%= @authorization.safe_app.name %>" <%= @previous_scopes_message %>. It was recently updated to include<%= " an" if @added_scopes.count == 1 %> additional <%= pluralize_without_number(@added_scopes.count, "scope") %> (<%= (@added_scopes.to_sentence) %>). Visit <%= settings_user_tokens_url %> for more information.
