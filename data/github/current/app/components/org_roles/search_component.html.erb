<%= render Primer::Box.new(flex: 1, classes: "assignment-list-query-builder") do %>
  <%= render(Primer::Experimental::QueryBuilder.new(
    id: "org-role-assignment-search-combobox",
    input_name: "query",
    label_text: "Search organization role assignees",
    leading_visual: { icon: :search },
    visually_hide_label: true,
    placeholder: "Search organization role assignments",
    form_action_url: url,
    value: query,
    "data-suggestable-roles": suggestable_roles,
  )) do |component| %>
    <% component.with_leading_visual_icon(icon: :search) %>
  <% end %>
<% end %>
