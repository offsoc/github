<%= render Primer::Alpha::TextField.new(
  caption: "#{AVOID_PII_NOTICE} Maximum #{IgnoredUser::MAX_NOTE_LENGTH} characters, "\
    "markdown supported. #{VISIBILITY_SCOPE_NOTICE % { visibility_scope: visibility_scope }}",
  full_width: true,
  label: "Add an optional note:",
  name: :note,
  value: @note,
  visually_hide_label: @visually_hide_label,
  id: @text_field_id,
  aria: { label: "Add an optional note:" },
  data: { test_selector: "ignored_user_note_text_field" },
) %>
