<% page_info title: "Codespaces - #{codespace.safe_display_name}", footer: false %>
<%= render Codespaces::ShowAuthComponent.new \
  codespace:,
  connection:,
  github_token:,
  github_token_valid_after:,
  user: current_user,
  user_settings:,
  cascade_token:,
  repository:,
  editor:
%>

