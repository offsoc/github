---
summary: Revoke an authorization for an application
description: OAuth application owners can revoke a single token for an OAuth application.
  You must use [Basic Authentication](${externalDocsUrl}/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication)
  for this method, where the username is the OAuth application `client_id` and the
  password is its `client_secret`.
operationId: oauth-authorizations/revoke-authorization-for-application
tags:
  - oauth-authorizations
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/oauth-authorizations/oauth-authorizations#revoke-an-authorization-for-an-application'
parameters:
  - $ref: '../../components/parameters/client-id.yaml'
  - $ref: '../../components/parameters/access-token.yaml'
responses:
  '204':
    description: Response
x-github-releases:
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: oauth-authorizations
  subcategory: oauth-authorizations
deprecated: true
x-github-internal:
  path: '/applications/{client_id}/tokens/{access_token}'
  http-method: delete
  service-tier: 2
  published: true
