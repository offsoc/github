---
summary: List GPG keys for a user
description: Lists the GPG keys for a user. This information is accessible by anyone.
tags:
  - users
operationId: users/list-gpg-keys-for-user
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/users/gpg-keys#list-gpg-keys-for-a-user'
parameters:
  - $ref: '../../components/parameters/username.yaml'
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/gpg-key.yaml'
        examples:
          default:
            $ref: '../../components/examples/gpg-key-items.yaml'
    headers:
      Link:
        $ref: '../../components/headers/link.yaml'
x-github-releases:
  - ghec
  - ghes: '>= 2.18'
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true

  category: users
  subcategory: gpg-keys
x-github-internal:
  path: '/users/{username}/gpg_keys'
  http-method: get
  service-tier: 2
  owner: '@github/external-identities'
  published: true
