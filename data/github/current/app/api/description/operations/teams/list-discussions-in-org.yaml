---
summary: List discussions
description: |-
  List all discussions on a team's page.

  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `GET /organizations/{org_id}/team/{team_id}/discussions`.

  OAuth app tokens and personal access tokens (classic) need the `read:discussion` scope to use this endpoint.
tags:
  - teams
operationId: teams/list-discussions-in-org
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/teams/discussions#list-discussions'
parameters:
  - $ref: '../../components/parameters/org.yaml'
  - $ref: '../../components/parameters/team-slug.yaml'
  - $ref: '../../components/parameters/direction.yaml'
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
  - name: pinned
    in: query
    required: false
    description: Pinned discussions only filter
    schema:
      type: string
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/team-discussion.yaml'
        examples:
          default:
            $ref: '../../components/examples/team-discussion-items.yaml'
    headers:
      Link:
        $ref: '../../components/headers/link.yaml'
x-github-releases:
  - ghec
  - ghes:
      - '>= 2.21'
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: teams
  subcategory: discussions
x-githubEnterpriseOverlays:
  '<= 3.2':
    x-github:
      previews:
        - $ref: '../../components/x-previews/squirrel-girl.yaml'
          required: false
x-github-internal:
  path: '/orgs/{org}/teams/{team_slug}/discussions'
  http-method: get
  service-tier: 2
  owner: '@github/meao'
  published: true
x-github-breaking-changes:
  - changeset: remove_team_discussions
    patch:
      response:
        '200':
