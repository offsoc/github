---
summary: Get the status of services running on all cluster nodes
description: |-
  Gets the status of all services running on each cluster node.
  This endpoint may take several seconds to reply.
operationId: enterprise-admin/get-cluster-status
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/manage-ghes#get-the-status-of-services-running-on-all-cluster-nodes'
servers:
  - url: '{protocol}://{hostname}'
    variables:
      hostname:
        default: 'HOSTNAME'
        description: Self-hosted Enterprise Server hostname
      protocol:
        default: 'http'
        description: 'Self-hosted Enterprise Server protocol'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/ghes-cluster-status.yaml'
        examples:
          default:
            $ref: '../../components/examples/ghes-cluster-status.yaml'
  '401':
    description: Unauthorized
  '500':
    description: Internal error
x-github-releases:
  - ghes: '>= 3.11'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: manage-ghes
x-github-internal:
  path: '/manage/v1/cluster/status'
  http-method: get
  service-tier: 1
  published: true
  owner: "@github/ghes-infrastructure"
