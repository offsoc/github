<%= render(Primer::Alpha::ActionMenu.new(anchor_align: :end, test_selector: "patreon-sponsorship-settings")) do |menu| %>
  <% menu.with_show_button(icon: :gear, scheme: :invisible, "aria-label": "Manage sponsorship settings") %>
  <% menu.with_item(
    **toggle_privacy_menu_item
  ) %>
  <% menu.with_item(
    **toggle_email_menu_item
  ) %>
<% end %>
