---
summary: List CODEOWNERS errors
description: |-
  List any syntax errors that are detected in the CODEOWNERS
  file.

  For more information about the correct CODEOWNERS syntax,
  see "[About code owners](${externalDocsUrl}/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners)."
tags:
  - repos
operationId: repos/codeowners-errors
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/repos/repos#list-codeowners-errors'
x-github-internal:
  path: '/repos/{owner}/{repo}/codeowners/errors'
  http-method: get
  published: true
  owner: '@github/pull-requests'
x-github-releases:
  - ghec
  - api.github.com
  - ghes:
      - '>= 3.5'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - name: ref
    description: "A branch, tag or commit name used to determine which version of the
      CODEOWNERS file to use. Default: the repository's default branch (e.g. `main`)"
    in: query
    required: false
    schema:
      type: string
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/codeowners-errors.yaml'
        examples:
          default:
            $ref: '../../components/examples/codeowners-errors.yaml'
  '404':
    description: Resource not found
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  previews: []
  category: repos
  subcategory: repos
