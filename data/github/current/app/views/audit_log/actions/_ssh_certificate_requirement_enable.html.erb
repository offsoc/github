Enabled the SSH certificate requirement for Git authentication for
<span class="context">
  <% if view.org? %>
    the <%= view.link_to_org %> organization
  <% else %>
    all organizations in the <%= view.link_to_business %> enterprise
  <% end %>
</span>
