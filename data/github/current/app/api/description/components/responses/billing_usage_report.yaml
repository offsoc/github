---
description: Response when getting a billing usage report
content:
  application/json:
    schema:
      $ref: '../schemas/billing-usage-report.yaml'
    examples:
          default:
            $ref: '../examples/billing-usage-report.yaml'
