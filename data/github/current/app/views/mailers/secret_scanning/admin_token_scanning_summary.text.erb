<%= @header_prefix %> <%= @repo.name_with_display_owner %>:

<%= @title %>
<%= @message %>
<% if @secondary_message.present? %>
<%= @secondary_message %>
<% end %>

<% @alerts.take(@alerts_list_limit).each do |alert| %>
  <%= alert.label %>
  <%= alert.first_location_text %>
  Review at <%= GitHub.url %>/<%= @repo.name_with_display_owner %>/security/secret-scanning/<%= alert.number %>

<% end %>
<% if @alerts_list_limit < @total_scan_count %>
and <%= @total_scan_count - @alerts_list_limit %> more alerts
<% end %>

This repository has a total of <%= @total_alert_count %> open secret scanning alerts

Review all at <%= GitHub.url %><%= @results_path %>

<% if @detected_by_ai %>
These secrets were detected by AI. False positive rates will improve over time.
<% end %>

<%= @footer %>

Thanks,
GitHub.com
