<%= render Primer::Beta::Blankslate.new(classes: "inbox-unavailable") do |c| %>
  <% c.with_visual_icon(icon: "alert") %>

  <% c.with_heading(tag: :h2) do %>
    Notifications, watching and settings are unavailable at the moment.
  <% end %>
<% end %>
