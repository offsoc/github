<%
  page_info(
    container_xl: true,
    responsive: true,
  )
  page_title "Forks · #{current_repository.name_with_display_owner}"
%>

<%= render layout: "layouts/repository/insights" do %>
  <%= render Forks::ForksHeaderComponent.new(path_resolver, root_repo_has_forks) %>
  <%= render Forks::EmptyForksComponent.new(current_repository, root_repo_has_forks, attributes[:last_updated].any?) %>
  <%= render Forks::ForksListingComponent.new(forks, attributes, path_resolver) %>
<% end %>
