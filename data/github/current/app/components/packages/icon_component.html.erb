<% if asset_path %>
  <%= svg(asset_path, width: @size, height: @size, class: Primer::Classify.call(**@args)[:class]) %>
<% else %>
  <%= render Primer::Beta::Octicon.new(**{icon: :package, height: @size}.merge(@args)) %>
<% end %>
