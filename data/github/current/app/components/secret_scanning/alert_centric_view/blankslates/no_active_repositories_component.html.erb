<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%= render(
  Primer::BlankslateComponent.new(
    icon: :repo,
    large: true,
    spacious: true,
    test_selector: "security-center-alert-centric-views-secret-scanning-alerts-blankslate-no-repos",
    title: "No repositories.",
    data: {hpc: ""}
  )
) do %>
  <%= render(Primer::Beta::Text.new(tag: :p)) do %>
    Your organization doesn't have any active repositories. Try
    <%= render(
      Primer::Beta::Link
        .new(
          data: { pjax: true },
          href: new_repository_path,
          classes: "Link--inTextBlock"
        )
        .with_content("creating one")
    ) %>.
  <% end %>
<% end %>
