<% @runner_groups.each do |runner_group| %>
  <%= render GitHub::Menu::ButtonComponent.new(
    text: runner_group.name,
    type: "submit",
    name: "runner_group_id",
    value: runner_group.id,
  ) %>
<% end %>
