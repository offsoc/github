<%= render(@item) do |item| %>
  <% if expires_at.present? %>
    <% item.with_trailing_visual_icon(icon: :clock, color: :success) %>
    <% item.with_description { "#{time_remaining} left" } %>
  <% end %>
<% end %>
