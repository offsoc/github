<%
  count = issue_or_pull_request.task_list_item_count
  complete_count = issue_or_pull_request.complete_task_list_item_count
%>

<% if count > 0 %>
  <%= render Issues::TrackedIssuesProgressComponent.new(mode: :inline, completed: complete_count, total: count) %>
<% end %>
