<ul class="filter-list mt-1">
  <% @gate_requests_by_environment.keys.each do |environment| %>
    <%= render Actions::Environments::RequestFormItemComponent.new(
      gate_requests: @gate_requests_by_environment[environment],
      can_break_glass: @can_break_glass) %>
  <% end %>
</ul>
