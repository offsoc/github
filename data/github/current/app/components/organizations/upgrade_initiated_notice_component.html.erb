<%# erblint:counter DeprecatedComponentsCounter 1 %>
<%= render(Primer::Beta::Flash.new(full: true, scheme: :warning, test_selector: "org-enterprise-upgrade-initiated-notice")) do %>
  An organization upgrade to GitHub Enterprise has been initiated but not completed.
  Return to the <%= link_to(
    'checkout page',
    billing_upgrade_from_organization_enterprise_path(
        @organization.upgrade_to_enterprise_in_progress),
        class: "Link--inTextBlock") %> to complete or cancel the upgrade.
<% end %>
