<% if results.total > 0 && results.includes_public_repositories? %>
  <div class="d-flex flex-justify-between border-bottom pb-3">
    <%= render partial: "codesearch/results_total",
               locals: { results: results, what: "topic result" } %>
  </div>
<% end %>
