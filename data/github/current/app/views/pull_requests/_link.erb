<%= render(Primer::Beta::Truncate.new(classes: "truncate-with-responsive-width", my: 1)) do |component| %>
  <% component.with_item(tag: :a, href: link.url, font_weight: :bold, classes: ["Link--primary", "markdown-title"]) do %>
    <%= render(Primer::Beta::Octicon.new(:link, size: :small)) %> <%= link.text %>
  <% end %>
<% end %>
