Updated default branch setting for new repositories
<% if (old_default_branch = view.entry.hit.data[:old_default_branch]).present? %>
  from <code><%= old_default_branch %></code>
<% end %>
<% if (new_default_branch = view.entry.hit.data[:new_default_branch]).present? %>
  to <code><%= new_default_branch %></code>
<% end %>
