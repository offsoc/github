Enabled IP allow list configuration for GitHub Apps installed on
<% unless view.org? %>
  organizations in
<% end %>
<span class="context">
  <% if view.org? %>
    the <%= view.link_to_org %> organization
  <% else %>
    the <%= view.link_to_business %> enterprise
  <% end %>
</span>
