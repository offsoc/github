Hello there,

@<%= @requester.display_login %> wants to transfer the following GitHub App:

<%= @integration.name %>

to your <%= @target.organization? ? "organization" : "account" %> <%= @target.safe_profile_name %>.

You can review and accept the transfer at the following link:

<%= gh_settings_app_transfer_url(@target, @xfer) %>

---
Sent with <3 by GitHub.
<%= GitHub.physical_address(multiline: true).join("\n") %>
