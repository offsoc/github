<% if view.entry.hit.data[:enablement] == "all-orgs-enabled" %>
  Updated Codespaces Enterprise Account policy to be enabled for all organizations
<% elsif view.entry.hit.data[:enablement] == "all-orgs-disabled" %>
  Updated Codespaces Enterprise Account policy to be disabled for all organizations
<% else %>
  Updated Codespaces Enterprise Account policy to be <%= view.entry.hit.data[:enablement] %> for the following organizations:
  <%= view.entry.hit.data[:organization_names].join(", ") %>
<% end %>
