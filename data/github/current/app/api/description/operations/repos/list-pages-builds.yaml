---
summary: List ${apiName} Pages builds
description: |-
  Lists builts of a ${apiName} Pages site.

  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
tags:
  - repos
operationId: repos/list-pages-builds
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/pages/pages#list-apiname-pages-builds'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/page-build.yaml'
        examples:
          default:
            $ref: '../../components/examples/page-build-items.yaml'
    headers:
      Link:
        $ref: '../../components/headers/link.yaml'
x-github-releases:
  - ghec
  - ghes: '>= 2.18'
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: pages
  subcategory: pages
x-github-internal:
  path: '/repos/{owner}/{repo}/pages/builds'
  http-method: get
  service-tier: 1
  owner: '@github/pages'
  published: true
