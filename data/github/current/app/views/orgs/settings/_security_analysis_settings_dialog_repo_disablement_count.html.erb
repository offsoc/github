<%# locals: (feature:, repo_count:) %>
<%= render Primer::Box.new do %>
  <%= render Primer::Beta::Text.new(tag: :p) do %>
    <strong>This will disable <%= feature %> on <%= number_with_delimiter(repo_count) %> <%= "repository".pluralize(repo_count) %> where it is enabled.</strong>
  <% end %>
<% end %>
