The page build failed <% if git_ref_name = @build&.deployment&.ref_name %>for the `<%= git_ref_name %>` branch<% end %> with the following error:

<%= raw @error %>

For information on troubleshooting Jekyll see:

  <%= GitHub.help_url %>/articles/troubleshooting-jekyll-builds

<% if GitHub.enterprise? %>
If you have any questions you can submit a request at https://enterprise.githubsupport.com/hc/en-us/requests/new
<% else %>
If you have any questions you can submit a request at <%= GitHub.contact_support_url(tags: "dotcom-pages", repo_id: @repository&.id, page_build_id: @build&.id) %>
<% end %>
