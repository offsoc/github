<% content_for :header do %>
  Your purchase of GitHub Enterprise was unsuccessful.
<% end %>

<%= render Mail::HeaderTitleComponent.new(
  title: "GitHub Enterprise trial account",
  avatar: @business
) %>

<p>
  Hi <strong><%= @actor.name %></strong>!
</p>
<p>
  Your purchase of GitHub Enterprise for the <%= @business.name %> enterprise was unsuccessful.
</p>
<p>
  To complete this purchase, verify your payment information is up-to-date and resubmit the order.
</p>

<%= render(Mail::SpacerComponent.new) %>

<%= render(Mail::ContainerComponent.new) do %>
  <%= render(
    Mail::ButtonComponent.new(
      text: "Buy GitHub Enterprise",
      url: billing_settings_upgrade_enterprise_url(@business),
      classes: "btn-primary btn-large"
    )
  ) %>
<% end %>

<%= render(Mail::SpacerComponent.new) %>

<p class="email-body-subtext">
  <strong>Note:</strong> If you get a 404 page, make sure you’re signed in as an owner of
  <strong><%= @business.name %></strong>.
</p>

<hr class="rule">

<p class="email-text-small email-text-gray">
  Button not working? Paste the following link into your browser:<br>
  <%= link_to(
    billing_settings_upgrade_enterprise_url(@business),
    billing_settings_upgrade_enterprise_url(@business)
  ) %>
</p>

<% content_for :footer_text do %>
  You’re receiving this email because you're an owner of the <%= @business.name %> GitHub Enterprise trial
  account on GitHub.
<% end %>
