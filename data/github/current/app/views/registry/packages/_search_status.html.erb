<% if has_package_query_params? %>
  <div class="mb-3">
    <%= link_to url_with(:q => nil, :ecosystem => nil, :visibility => nil, :repo_name => nil, :sort_by => nil), :class => "issues-reset-query" do %>
      <%= primer_octicon(:x, classes: "issues-reset-query-icon flex-shrink-0") %>
      Clear current search query, filters, and sorts
    <% end %>
  </div>
<% end %>