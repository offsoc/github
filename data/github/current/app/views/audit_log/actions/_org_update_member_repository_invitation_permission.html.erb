<span class="context"><%= (!!view.entry.hit.data[:permission]) ? "Allowed" : "Prevented" %></span> members to invite <%= outside_collaborators_verbiage(view.entry.hit[:org]) %> to repositories in the <span class="context"><%= view.entry.safe_organization_name %></span> organization
