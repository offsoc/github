<% if view.entry.hit.data[:is_republished] %>
  Republished version <%= view.entry.hit.data[:version] %> to the <%= view.ecosystem %> package <%= view.entry.hit.data[:package] %>
<% else %>
  Published version <%= view.entry.hit.data[:version] %> to the <%= view.ecosystem %> package <%= view.entry.hit.data[:package] %>
<% end %>
