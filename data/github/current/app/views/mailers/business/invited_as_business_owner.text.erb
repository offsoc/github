Hi<%= " #{@user.safe_profile_name}" if @user.present? %>,

<% if @show_inviter %>@<%= @inviter.display_login %> has invited you<% else %>You’re invited<% end %> to become an owner of the <%= @business.name %> enterprise on GitHub.<% if @user.present? %> Head over to <%= enterprise_url @business %> to check out the <%= @business.name %> profile.<% end %>

<% if @reinvited_count > 0 %>
You are being invited again because your previous invitation expired.
<% end %>

This invitation will expire in <%= pluralize(GitHub.invitation_expiry_period, "day") %>.

To become an owner of <%= @business.name %>, follow this link:

<% if @invitation_token.present? %>
<%= enterprise_owner_invitation_url(@business, invitation_token: @invitation_token) %>
<% else %>
<%= enterprise_owner_invitation_url(@business) %>
<% end %>

<% if @user.present? %>
Some helpful tips:

- If you get a 404 page, make sure you’re signed in as <%= @user.display_login %>.
- You can also accept the invitation by visiting the enterprise page directly at <%= enterprise_url @business %>
<% end %>

If you were not expecting this invitation, you can ignore this email.

Thanks,
The GitHub Team
