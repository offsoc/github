<%= primer_form_with model: MemexProject.new, url: create_path, method: :post, data: { turbo: false } do |f| %>
  <%= render(Memex::ProjectList::AddProjectButton.new(f, user: current_user, is_template: @is_template, ui: @ui)) %>
<% end %>
