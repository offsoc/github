---
summary: Get repository clones
description: Get the total number of clones and breakdown per day or week for the
  last 14 days. Timestamps are aligned to UTC midnight of the beginning of the day
  or week. Week begins on Monday.
tags:
  - repos
operationId: repos/get-clones
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/metrics/traffic#get-repository-clones'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - $ref: '../../components/parameters/per.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/clone-traffic.yaml'
        examples:
          default:
            $ref: '../../components/examples/clone-traffic.yaml'
  '403':
    $ref: '../../components/responses/forbidden.yaml'
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: metrics
  subcategory: traffic
x-github-internal:
  path: '/repos/{owner}/{repo}/traffic/clones'
  http-method: get
  service-tier: 1
  owner: '@github/insights'
  published: true
