Git LFS has been disabled on <%= @user.organization? ? "the organization" : "your personal account" %> <%= @user %> because you’ve exceeded your data plan by at least 150%. Please purchase additional data packs to cover your bandwidth and storage usage:

  <%= @user.organization? ? "#{GitHub.url}/organizations/#{@user}/billing/data/upgrade" : "#{GitHub.url}/account/billing/data/upgrade" %>

Current usage as of <%= @asset_status.updated_at.strftime("%d %b %Y %I:%M%p %Z") %>:

  Bandwidth: <%= @asset_status.bandwidth_usage %> GB / <%= @asset_status.bandwidth_quota.round %> GB (<%= @asset_status.bandwidth_usage_percentage %>%)
  Storage: <%= @asset_status.storage_usage %> GB / <%= @asset_status.storage_quota.round %> GB (<%= @asset_status.storage_usage_percentage %>%)
