---
summary: List codespaces for the organization
description: |-
  Lists the codespaces associated to a specified organization.

  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
tags:
  - codespaces
operationId: codespaces/list-in-organization
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/codespaces/organizations#list-codespaces-for-the-organization'
parameters:
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/page.yaml'
  - $ref: '../../components/parameters/org.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: object
          required:
            - total_count
            - codespaces
          properties:
            total_count:
              type: integer
            codespaces:
              type: array
              items:
                $ref: '../../components/schemas/codespace.yaml'
        examples:
          default:
            $ref: '../../components/examples/codespaces-list.yaml'
  '304':
    $ref: '../../components/responses/not_modified.yaml'
  '500':
    $ref: '../../components/responses/internal_error.yaml'
  '401':
    $ref: '../../components/responses/requires_authentication.yaml'
  '403':
    $ref: '../../components/responses/forbidden.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
x-github-resource-owner: '@github/codespaces-reviewers'
x-github-internal:
  owner: '@github/codespaces-team'
  path: /orgs/{org}/codespaces
  http-method: get
  published: true
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: codespaces
  subcategory: organizations
