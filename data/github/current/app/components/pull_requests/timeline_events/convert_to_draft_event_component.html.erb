<%= render(PullRequests::TimelineEvents::TimelineItemComponent.new(issue_event: issue_event, icon: "git-pull-request-draft")) do |component| %>
  <% component.with_body do %>
    marked this pull request as draft
  <% end %>
<% end %>
