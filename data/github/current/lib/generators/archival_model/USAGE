Description:
    Set up archival model and table for a given ActiveRecord model.

Example:
    rails generate archival_model Thing

    This will create:
      + db/migrate/create_archived_things.rb
      + app/models/archived/thing.rb
      + test/models/archived/thing_test.rb
