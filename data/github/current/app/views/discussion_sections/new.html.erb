<%
  page_info title: "New Discussion Section · #{ is_org_level? ? this_organization.display_login : current_repository.name_with_display_owner }",
    container_xl: true,
    selected_link: :repo_discussions
%>
<%= render layout: "discussions/conditional_discussions_layout" do %>
  <div class="container-md px-3 px-md-4 px-lg-5">
    <div class="d-flex flex-justify-between flex-items-baseline mb-3">
      <%= render(Primer::Beta::Heading.new(tag: :h1, font_size: 2)) { "Create section" } %>
    </div>
    <%= render Discussions::SectionFormComponent.new(
      section: section,
      repository: current_repository,
      org_param: is_org_level? ? this_organization.to_param : nil,
    ) %>
  </div>
<% end %>
