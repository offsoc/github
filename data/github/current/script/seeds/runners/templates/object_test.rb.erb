# frozen_string_literal: true

require "test_helper"
require_relative Rails.root.join("script/seeds/objects/<%= name.underscore %>")

class SeedsObjects<%= name.camelize %>Test < GitHub::TestCase
  test "the truth", skip_enterprise: true do
  end
end
