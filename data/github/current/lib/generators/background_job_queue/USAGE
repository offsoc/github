Description:
    Generates a new background job queue configuration

Examples:
    bin/rails generate background_job_queue my_queue --service=some_service --dotcom-worker-type=high --enterprise-priority=high --staff --development

    This will create (or append to):
        config/background_job_queues/some_service.yml

    bin/rails generate background_job_queue my_queue

    This will prompt for service name, dotcom, enterprise, staff environment, and development environment configuration
