<%= render Primer::Beta::Counter.new(
      count: count,
      hide_if_zero: true,
      round: true,
      id: "#{label.parameterize}-repo-tab-count",
      data: {
        pjax_replace: "",
        turbo_replace: "",
      },
    )
%>
