---
summary: 'Remove all organization roles for a user'
description: |-
  Revokes all assigned organization roles from a user. For more information on organization roles, see "[Using organization roles](${externalDocsUrl}/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."

  The authenticated user must be an administrator for the organization to use this endpoint.

  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
tags:
  - orgs
operationId: orgs/revoke-all-org-roles-user

externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/orgs/organization-roles#remove-all-organization-roles-for-a-user'

x-github-internal:
  path: /orgs/{org}/organization-roles/users/{username}
  http-method: delete
  published: true
  owner: '@github/authorization'

x-github-releases:
  - api.github.com
  - ghec
  - ghes: '>= 3.14'

parameters:
 - $ref: '../../components/parameters/org.yaml'
 - $ref: '../../components/parameters/username.yaml'

responses:
  '204':
    description: Response

x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: orgs
  subcategory: organization-roles
