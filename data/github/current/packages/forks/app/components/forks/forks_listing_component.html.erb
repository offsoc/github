<%= render(Primer::Beta::BorderBox.new(mt: 3)) do |box| %>
  <% detail_components.each do |c| %>
    <% box.with_row { render(c) } %>
  <% end %>
<% end %>
<%= render Primer::Box.new(text_align: :center, mt: 4) do %>
  <%= will_paginate forks %>
<% end %>
