<%= render(GitHub::Menu::RadioComponent.new(
  text: repository.name, checked: checked, id: "repository-#{repository.name}",
  name: "repository", value: repository.name,
  input_classes: "js-pat-owner-search-filter", octicon: octicon_for(repository),
  data: { "test-selector" => "org-repositories", "test-id" => repository.id.to_s }
)) %>
