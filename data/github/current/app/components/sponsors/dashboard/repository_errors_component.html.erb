<%= render(Primer::Box.new(
      bg: :danger,
      p: 2,
      mb: 3,
      border: true,
      border_radius: 1,
      border_color: :danger,
      test_selector: "repository-errors")) do %>
  <%= render(Primer::Beta::Octicon.new(icon: :alert, color: :danger)) %>
  New sponsors cannot be added to the selected repository.
  (Repository <%= errors.to_sentence %>).
<% end %>
