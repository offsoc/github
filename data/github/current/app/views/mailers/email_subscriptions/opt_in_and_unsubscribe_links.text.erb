Double opt-in links:
<% @opt_in_links.each do |link| %>
  <%= link[:title] %>: <%= link[:url] %>
<% end %>

Unsubscribe links:
<% @unsubscribe_links.each do |link| %>
  <%= link[:title] %>: <%= link[:url] %>
<% end %>
