---
summary: Get the customization template for an OIDC subject claim for a repository
description: |-
  Gets the customization template for an OpenID Connect (OIDC) subject claim.

  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
tags:
  - actions
operationId: actions/get-custom-oidc-sub-claim-for-repo
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/actions/oidc#get-the-customization-template-for-an-oidc-subject-claim-for-a-repository'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
responses:
  '200':
    description: Status response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/oidc-custom-sub-repo.yaml'
        examples:
          default:
            $ref: '../../components/examples/oidc-custom-sub-repo.yaml'
  '400':
    $ref: '../../components/responses/bad_request.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
x-github-releases:
  - api.github.com
  - ghec
  - ghes:
      - '>= 3.7'
x-github:
  enabledForGitHubApps: true
  previews: []
  category: actions
  subcategory: oidc
x-github-internal:
  path: '/repos/{owner}/{repo}/actions/oidc/customization/sub'
  http-method: get
  service-tier: 1
  owner: '@github/actions-fusion-reviewers'
  published: true
