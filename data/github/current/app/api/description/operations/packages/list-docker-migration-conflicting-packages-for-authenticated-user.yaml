---
summary: 'Get list of conflicting packages during Docker migration for authenticated-user'
description: |-
  Lists all packages that are owned by the authenticated user within the user's namespace, and that encountered a conflict during a Docker migration.

  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
tags:
  - packages
operationId: packages/list-docker-migration-conflicting-packages-for-authenticated-user
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/packages/packages#get-list-of-conflicting-packages-during-docker-migration-for-authenticated-user'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/package.yaml'
        examples:
          default:
            $ref: '../../components/examples/packages-for-user.yaml'
x-github-releases:
  - ghec
  - api.github.com
  - ghes: '>= 3.9'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: packages
  subcategory: packages
x-github-internal:
  path: '/user/docker/conflicts'
  http-method: get
  service-tier: 1
  owner: '@github/package-registry'
  published: true
