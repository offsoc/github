---
# TODO: auto generate this using the upstream protos
title: Sigstore Bundle v0.1
description: Sigstore Bundle v0.1
type: object
properties:
  mediaType:
    type: string
  verificationMaterial:
    type: object
    properties:
      x509CertificateChain:
        type: object
        properties:
          certificates:
            type: array
            items:
              type: object
              properties:
                rawBytes:
                  type: string
      tlogEntries:
        type: array
        items:
          type: object
          properties:
            logIndex:
              type: string
            logId:
              type: object
              properties:
                keyId:
                  type: string
            kindVersion:
              type: object
              properties:
                kind:
                  type: string
                version:
                  type: string
            integratedTime:
              type: string
            inclusionPromise:
              type: object
              properties:
                signedEntryTimestamp:
                  type: string
            inclusionProof:
              type: string  # TODO is this an object?
              nullable: true
            canonicalizedBody:
              type: string
      timestampVerificationData:
        type: string  # TODO: is this an object?
        nullable: true
  dsseEnvelope:
    type: object
    properties:
      payload:
        type: string
      payloadType:
        type: string
      signatures:
        type: array
        items:
          type: object
          properties:
            sig:
              type: string
            keyid:
              type: string
