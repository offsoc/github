This email is to confirm that the ‘<%= @org_login %>’ organization has been archived on GitHub<% if @archiver.present? && !@by_site_admin %> by ‘<%= @archiver %>’<% end %>.

Your organization’s repositories have been archived and most write/change operations are now disabled.
