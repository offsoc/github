<%= render Site::CardComponent.new(
  header: @title,
  heading_size: :h2,
  text: helpers.contentful_rich_text(@text),
  url: @link,
  data: {
    test_selector: "contentful-resource-card-component"
  }
) %>
