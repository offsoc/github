# format is: [address]:port username:password
<%
   smtp_config = @config['smtp']
   if smtp_config && smtp_config['enabled']
-%>
[<%= smtp_config['address'] %>]:<%= smtp_config['port'] || 25 %> <%= smtp_config['username'] %>:<%= smtp_config['password'] %>
<% end -%>
