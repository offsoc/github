---
type: object
properties:
  enabled:
    $ref: 'actions-enabled.yaml'
  allowed_actions:
    $ref: 'allowed-actions.yaml'
  selected_actions_url:
    $ref: 'selected-actions-url.yaml'
required:
  - enabled
