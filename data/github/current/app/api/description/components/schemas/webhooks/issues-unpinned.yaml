---
title: issues unpinned event
type: object
properties:
  action:
    type: string
    enum:
      - unpinned
  enterprise:
    $ref: '../enterprise-webhooks.yaml'
  installation:
    $ref: '../simple-installation.yaml'
  issue:
    $ref: ../webhooks_issue_2.yaml
  organization:
    $ref: '../organization-simple-webhooks.yaml'
  repository:
    $ref: '../repository-webhooks.yaml'
  sender:
    $ref: '../simple-user-webhooks.yaml'
required:
  - action
  - issue
  - repository
  - sender
