# Example mutation for testing purposes. Do not use in production.
mutation CreateProject($id: ID!, $name: String!) {
  createProject(input: {ownerId: $id, name: $name}) {
    project {
      id
    }
  }
}
