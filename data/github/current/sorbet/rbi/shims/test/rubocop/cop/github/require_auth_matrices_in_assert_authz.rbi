# typed: true

module RuboCop
  module Cop
    module GitHub
      class RequireAuthMatricesInAssertAuthz < Base

        def authz_test?(node)
        end

        def authz_assertions(node)
        end

        def specify_auth_matrices?(node)
        end

        def local_matrix_instantiation(node)
        end

        def local_variable_name(node)
        end

        def asserted_actors(node)
        end

        def matrix_actor(node)
        end
      end
    end
  end
end
