<%= render(Primer::Alpha::Banner.new(classes: "js-flash-new-token", mb: 3)) do |c| %>
  <% c.with_action_content do %>
    <%= render(Primer::Beta::Button.new(tag: :a, href: enterprise_getting_started_path(current_user.enterprise_managed_business), float: :right, test_selector: "continue_setup_btn")) { "Continue setup" } %>
  <% end if current_user.is_first_emu_owner? %>
  Make sure to copy your personal access token now. You won’t be able to see it again!
<% end %>
