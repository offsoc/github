<%= render(Primer::Alpha::SelectPanel.new(
  id: "actions_actors_filter",
  title: "Filter by Actor",
  src: actions_actors_menu_path(selected_filename: selected_workflow&.filename, lab: selected_workflow&.lab?, query: actions_filtered_query(filters: workflow_run_filters)),
  no_results_label: "No matching users."
)) do |menu| %>
  <% menu.with_show_button(scheme: :invisible, color: :muted, font_weight: :normal) do |button| %>
    <% button.with_trailing_action_icon(icon: :"triangle-down", display: [:none, :block]) %>
    Actor
  <% end %>
<% end %>
