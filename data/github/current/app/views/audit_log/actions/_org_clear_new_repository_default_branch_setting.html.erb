Cleared default branch setting
<% if (old_default_branch = view.entry.hit.data[:old_default_branch]).present? %>
  of <code><%= old_default_branch %></code>
<% end %>
for new repositories in the
<% if view.org? %>
  <%= view.link_to_org %> organization
<% else %>
  <%= view.link_to_business %> enterprise
<% end %>
