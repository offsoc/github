<% from_api = view.entry.data["request_category"] == "api" %>
Runners <%= view.entry.hit.data[:runner_ids].join(",") %> added to runner group <%= view.entry.hit.data[:runner_group_id] %> <%= "#{view.entry.hit.data[:runner_group_name]} " if view.entry.hit.data[:runner_group_name].present? %>for <span class="context"><%= view.link_to_business %></span><% if from_api %> via the API <% end %>
