import {renderHook} from '@testing-library/react'
import { {{~hookName~}} } from '../{{packageName}}'

test('Renders the {{hookName}} hook', () => {
  const {result} = renderHook(() => {{hookName}}())
  expect(result).toBe('hello world')
})
