<%= render(GitHub::FlashActionDismissibleComponent.new(level: :default, dismissible_path: dismissal_path, test_selector: "depleted_prepaid_credits", mb: 4)) do |c| %>
  <% c.with_text do %>
    You have used all of your prepaid credit. Usage charges for metered services will now be added towards your next invoice.
  <% end %>
<% end %>
