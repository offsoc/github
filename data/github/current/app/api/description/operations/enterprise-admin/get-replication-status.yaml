---
summary: Get the status of services running on all replica nodes
description: |-
  Gets the status of all services running on each replica node.
  This endpoint may take several seconds to reply.
operationId: enterprise-admin/get-replication-status
tags:
  - enterprise-admin
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/enterprise-admin/manage-ghes#get-the-status-of-services-running-on-all-replica-nodes'
servers:
  - url: '{protocol}://{hostname}'
    variables:
      hostname:
        default: 'HOSTNAME'
        description: Self-hosted Enterprise Server hostname
      protocol:
        default: 'http'
        description: 'Self-hosted Enterprise Server protocol'
parameters:
  - $ref: '../../components/parameters/uuid.yaml'
  - $ref: '../../components/parameters/cluster-roles.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/ghes-replication-status.yaml'
        examples:
          default:
            $ref: '../../components/examples/ghes-replication-status.yaml'
  '401':
    description: Unauthorized
  '500':
    description: Internal error
x-github-releases:
  - ghes: '>= 3.9'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: false
  category: enterprise-admin
  subcategory: manage-ghes
x-github-internal:
  path: '/manage/v1/replication/status'
  http-method: get
  service-tier: 1
  published: true
  owner: "@github/ghes-infrastructure"
