---
description: Response
content:
  application/json:
    schema:
      type: object
      required:
        - total_count
        - labels
      properties:
        total_count:
          type: integer
        labels:
          type: array
          items:
            $ref: '../schemas/runner-label.yaml'
    examples:
      default:
        $ref: '../examples/runner-labels.yaml'
