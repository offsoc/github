#!/bin/bash

set -e
cd $(dirname "$0")/..

rm -rf .bundle/config
rm -rf .bundle/ruby
bundle config --local path .bundle
bundle config --local build.nokogiri ""

# Always update since Gemfile.lock is not committed
bundle update "$@"
