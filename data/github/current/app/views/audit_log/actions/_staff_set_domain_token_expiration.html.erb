<% entry = view.entry
owner_link, owner_type = if entry.hit[:owner_type] == "User"
  [view.link_to_org(entry.hit[:owner]), "organization"]
elsif entry.hit[:owner_type] == "Business"
  [view.link_to_business(entry.hit[:owner]), "enterprise"]
end %>
GitHub staff set the verification code expiry time for the <%= entry.hit[:domain_name] %> domain
owned by the <%= owner_link %> <%= owner_type %> to <%= entry.hit[:token_expires_at] %>.
