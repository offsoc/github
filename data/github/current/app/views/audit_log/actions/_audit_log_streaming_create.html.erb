<% case view.entry.hit[:audit_log_stream_sink] %>
<% when "azure_blob" %>
  Configured Azure Blob Storage endpoint.
<% when "azure_hubs" %>
  Configured Azure Event Hubs endpoint.
<% when "datadog" %>
  Configured Datadog endpoint.
<% when "google_cloud" %>
  Configured Google Cloud Storage endpoint.
<% when "s3" %>
  Configured Amazon S3 endpoint.
<% when "splunk" %>
  Configured Splunk endpoint.
<% else %>
  Configured view.entry.hit[:audit_log_stream_sink] endpoint.
<% end %>
