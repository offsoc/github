<% page_info title: "#{billable_entity} - Invoices" %>

<%= render Billing::Settings::PastInvoicesComponent.new(
      target: billable_entity,
      invoices: invoices,
      can_make_payment: can_make_payment,
      invoice_path_builder: method(:stafftools_user_billing_show_invoice_path)
    ) %>

