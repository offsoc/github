#!/bin/bash

# Show usage directly from `dpages`
if [ "$1" = "--help" ] || [ "$1" = "-h" ]; then
  github-env env PROGRAM_NAME=ghe-dpages bin/dpages 2>&1 || true
  exit 0
fi

github-env env PROGRAM_NAME=ghe-dpages bin/dpages "$@"