---
summary: List CodeQL databases for a repository
description: |-
  Lists the CodeQL databases that are available in a repository.

  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
tags:
  - code-scanning
operationId: code-scanning/list-codeql-databases

externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/code-scanning/code-scanning#list-codeql-databases-for-a-repository'

x-github-resource-owner: '@github/code-scanning-secexp'

x-github-internal:
  owner: '@github/code-scanning-secexp'
  path: /repos/{owner}/{repo}/code-scanning/codeql/databases
  http-method: get
  published: true

x-github-releases:
  - ghec
  - api.github.com

parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/code-scanning-codeql-database.yaml'
        examples:
          default:
            $ref: '../../components/examples/code-scanning-codeql-databases.yaml'
  '403':
    $ref: '../../components/responses/code_scanning_forbidden_read.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
  '503':
    $ref: '../../components/responses/service_unavailable.yaml'

x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  previews: []
  category: code-scanning
  subcategory: code-scanning
