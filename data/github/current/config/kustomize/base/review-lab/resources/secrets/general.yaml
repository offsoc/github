apiVersion: review-lab/v1
kind: Secret
metadata:
  name: general
data:
  secret-key-base:
    valueFrom:
      shell:
        command: ["openssl", "rand", "-hex", "32"]
  longpoll-socket-id-secret:
    valueFrom:
      shell:
        command: ["openssl", "rand", "-hex", "32"]

