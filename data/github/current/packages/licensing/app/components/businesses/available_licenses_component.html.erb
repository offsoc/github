<% if render_available_licenses? %>
  <p class="f6 text-center">
    <strong><%= business.name %></strong> has
    <strong <%= test_selector "available-licenses" %>><%= number_with_delimiter available_licenses %></strong>
    <%= pluralize_without_number available_licenses, "available license" %>.
  </p>
<% end %>
