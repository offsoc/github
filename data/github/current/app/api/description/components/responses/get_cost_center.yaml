---
description: Response when getting a cost center
content:
  application/json:
    schema:
      $ref: '../schemas/get-cost-center.yaml'
    examples:
          default:
            $ref: '../examples/get-cost-center.yaml'
