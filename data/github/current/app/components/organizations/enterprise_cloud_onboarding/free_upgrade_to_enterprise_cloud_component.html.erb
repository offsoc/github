<%= render Primer::Box.new(**system_arguments, display: :flex, direction: :column) do %>
  <%= render(Primer::Beta::Text.new(font_size: 4, font_weight: :bold, mb: 1).with_content("Upcoming automatic upgrade to Enterprise account")) %>
  <%= render(Primer::Beta::Text.new(color: :muted, mb: 2)) do %>
    <%= current_organization.present? ? "This organization is" : "These organizations are" %> currently on the <%= enterprise_cloud_plan_link %>.
    Starting September 3, 2024, we will automatically upgrade all Enterprise Cloud to the Enterprise Account experience <strong>free of charge</strong>.
  <% end %>
  <%= render(Primer::Beta::Text.new(color: :muted, mb: 2)) do %>
    An enterprise account enables scalability as well as collaboration across multiple organizations. It allows owners to centrally manage policy, billing, and security for these organizations. <%= learn_more_link %>.
  <% end %>
  <%= render(Primer::Beta::Text.new(color: :muted)) do %>
    Upgrade now to claim your enterprise name early and unlock more features instantly.
  <% end %>
<% end %>
