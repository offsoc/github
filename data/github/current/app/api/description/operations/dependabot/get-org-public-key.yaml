---
summary: Get an organization public key
description: |-
  Gets your public key, which you need to encrypt secrets. You need to
  encrypt a secret before you can create or update secrets.

  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
tags:
  - dependabot
operationId: dependabot/get-org-public-key
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/dependabot/secrets#get-an-organization-public-key'
parameters:
  - $ref: '../../components/parameters/org.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/dependabot-public-key.yaml'
        examples:
          default:
            $ref: '../../components/examples/dependabot-public-key.yaml'
x-github-releases:
  - ghec
  - api.github.com
  - ghes:
      - '>= 3.4'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: dependabot
  subcategory: secrets
x-github-internal:
  path: '/orgs/{org}/dependabot/secrets/public-key'
  http-method: get
  service-tier: 2
  owner: '@github/dependabot-updates'
  published: true
