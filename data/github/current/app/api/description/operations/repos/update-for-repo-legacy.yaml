---
summary: Update
description: ''
tags:
  - repos
operationId: repos/update-for-repo-legacy
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/reference/repos#update'
parameters:
  - name: repository_id
    in: path
    required: true
    schema:
      type: string
  - $ref: '../../components/parameters/pre-receive-hook-id.yaml'
requestBody:
  content:
    application/json:
      schema:
        properties:
          name:
            description: The name of the pre-receive hook
            example: Commit message enforcement
            type: string
          script:
            description: Path to the script that will be executed when this hook runs
            example: scripts/commit_enforcements.sh
            type: string
          environment:
            description: The pre-receive environment the script will be executed in
            type: object
            properties:
              id:
                description: Unique identifier of the pre-receive environment
                example: 42
                type: integer
            required:
              - id
          script_repository:
            description: The repository where the script resides
            type: object
            properties:
              full_name:
                description: owner and name of this repo
                example: github/linguist
                type: string
            required:
              - full_name
          enforcement:
            type: string
            enum:
              - enabled
              - disabled
              - testing
          allow_downstream_configuration:
            type: boolean
        type: object
        nullable: true
responses:
  '200':
    description: TBD
x-github-releases: []
x-github-internal:
  path: '/repositories/{repository_id}/pre-receive-hooks/{pre_receive_hook_id}'
  http-method: post
  service-tier: 1
  published: true
x-github-breaking-changes:
  - changeset: change_pre_receive_hook_id_path_parameter_to_string
    patch:
      - op: replace
        path: '/parameters/1/schema/type'
        value: string
