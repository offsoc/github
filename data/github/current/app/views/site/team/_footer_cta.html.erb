<% event_source = "team-page-footer" if local_assigns[:event_source].nil? %>

<%= render Site::FooterCtaComponent.new(
  header: "Build like the best",
  text: "Get the complete developer platform",
  id: event_source,
  overflow_visible: true
) do %>
  <%=
    render partial: "site/team/get_started_cta",
    locals: { event_source: event_source }
  %>
<% end %>
