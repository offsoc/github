---
type: array
description: A list of default code security configurations
items:
  type: object
  properties:
    default_for_new_repos:
      enum:
      - public
      - private_and_internal
      - all
      description: The visibility of newly created repositories for which the code security configuration
        will be applied to by default
    configuration:
      $ref: './code-security-configuration.yaml'
