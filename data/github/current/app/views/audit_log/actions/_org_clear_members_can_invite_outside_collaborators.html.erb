<% if view.org? %>
    The setting for "Members can invite <%= outside_collaborators_verbiage(view.entry.hit[:org]) %>" has been cleared for the <%= view.link_to_org %> organization.
<% else %>
    The setting for "Members can invite <%= outside_collaborators_verbiage(view.entry.hit[:business]) %>" has been cleared for the <%= view.link_to_business %> enterprise.
<% end %>
