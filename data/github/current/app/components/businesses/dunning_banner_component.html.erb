<%= render(Primer::Alpha::Banner.new(full: true, scheme: :warning, icon: :alert)) do |component| %>
  <span class="text-bold">
    We are having a problem billing the <%= business.name %> enterprise. Please
    <%= link_to("update your payment method", settings_billing_tab_enterprise_path(business, tab: :payment_information), class: "Link--inTextBlock") %>
    or call your payment provider for details on why the transaction failed.
  </span>
  You can always <a class="Link--inTextBlock" href="<%= contact_path %>">contact support</a> with any questions.
<% end %>
