<% if view.email_invitation? %>
  Canceled the invitation to <span class="context"><%= view.entry.hit.data[:email] %></span> for the <span class="context"><%= view.entry.safe_organization_name %></span> organization
<% else %>
  Canceled <%= view.link_to_user %>’s invitation to the <span class="context"><%= view.entry.safe_organization_name %></span> organization
<% end %>
