---
summary: List code scanning alerts for a repository
description: |-
  Lists code scanning alerts.

  The response includes a `most_recent_instance` object.
  This provides details of the most recent instance of this alert
  for the default branch (or for the specified Git reference if you used `ref` in the request).

  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
tags:
  - code-scanning
operationId: code-scanning/list-alerts-for-repo
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/code-scanning/code-scanning#list-code-scanning-alerts-for-a-repository'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - $ref: '../../components/parameters/tool-name.yaml'
  - $ref: '../../components/parameters/tool-guid.yaml'
  - $ref: '../../components/parameters/page.yaml'
  - $ref: '../../components/parameters/per-page.yaml'
  - $ref: '../../components/parameters/git-ref.yaml'
  - $ref: '../../components/parameters/direction.yaml'
  - name: sort
    description: The property by which to sort the results.
    in: query
    required: false
    schema:
      type: string
      enum:
        - created
        - updated
      default: created
  - name: state
    description: If specified, only code scanning alerts with this state will be returned.
    in: query
    required: false
    schema:
      $ref: '../../components/schemas/code-scanning-alert-state-query.yaml'
  - name: severity
    description: If specified, only code scanning alerts with this severity will be returned.
    in: query
    required: false
    schema:
      $ref: '../../components/schemas/code-scanning-alert-severity.yaml'
  - name: after
    description: 'A cursor, as given in the [Link header](${externalDocsUrl}/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](${externalDocsUrl}/rest/using-the-rest-api/using-pagination-in-the-rest-api)."'
    in: query
    required: false
    schema:
      type: string
    x-unpublished: parameter is gated behind FF :code_scanning_repo_alerts_cursor_pagination
  - name: before
    description: 'A cursor, as given in the [Link header](${externalDocsUrl}/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](${externalDocsUrl}/rest/using-the-rest-api/using-pagination-in-the-rest-api)."'
    in: query
    required: false
    schema:
      type: string
    x-unpublished: parameter is gated behind FF :code_scanning_repo_alerts_cursor_pagination

responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/schemas/code-scanning-alert-items.yaml'
        examples:
          default:
            $ref: '../../components/examples/code-scanning-alert-items.yaml'
  '304':
    $ref: '../../components/responses/not_modified.yaml'
  '403':
    $ref: '../../components/responses/code_scanning_forbidden_read.yaml'
  '404':
    $ref: '../../components/responses/not_found.yaml'
  '503':
    $ref: '../../components/responses/service_unavailable.yaml'
x-githubEnterpriseOverlays:
  '< 3.8':
    parameters:
      - $ref: '../../components/parameters/owner.yaml'
      - $ref: '../../components/parameters/repo.yaml'
      - $ref: '../../components/parameters/tool-name.yaml'
      - $ref: '../../components/parameters/tool-guid.yaml'
      - $ref: '../../components/parameters/page.yaml'
      - $ref: '../../components/parameters/per-page.yaml'
      - $ref: '../../components/parameters/git-ref.yaml'
      - $ref: '../../components/parameters/direction.yaml'
      - name: sort
        description: The property by which to sort the results.
        in: query
        required: false
        schema:
          type: string
          enum:
            - created
            - updated
          default: created
      - name: state
        description: If specified, only code scanning alerts with this state will be returned.
        in: query
        required: false
        schema:
          $ref: '../../components/schemas/code-scanning-alert-state.yaml'
  '< 3.4':
    description: |-
      Lists all open code scanning alerts for the default branch (usually `main`
      or `master`). You must use an access token with the `security_events` scope to use
      this endpoint.

      The response includes a `most_recent_instance` object.
      This provides details of the most recent instance of this alert
      for the default branch or for the specified Git reference
      (if you used `ref` in the request).
    parameters:
      - $ref: '../../components/parameters/owner.yaml'
      - $ref: '../../components/parameters/repo.yaml'
      - $ref: '../../components/parameters/tool-name.yaml'
      - $ref: '../../components/parameters/tool-guid.yaml'
      - $ref: '../../components/parameters/page.yaml'
      - $ref: '../../components/parameters/per-page.yaml'
      - $ref: '../../components/parameters/git-ref.yaml'
      - name: state
        description: Set to `open`, `fixed`, or `dismissed` to list code scanning alerts
          in a specific state.
        in: query
        required: false
        schema:
          $ref: '../../components/schemas/code-scanning-alert-state.yaml'
    responses:
      '304':
x-github-releases:
  - ghec
  - api.github.com
  - ghes:
      - '>= 2.22'
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true

  category: code-scanning
  subcategory: code-scanning
x-github-internal:
  path: '/repos/{owner}/{repo}/code-scanning/alerts'
  http-method: get
  service-tier: 2
  owner: '@github/code-scanning-experiences'
  published: true
