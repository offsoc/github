<%= primer_form_with url: @action, method: :put, class: "my-6" do |f| %>
  <h3 class="text-normal mb-2" <%= test_selector("fork-pr-approvals-component") %>>Fork pull request workflows from outside collaborators</h3>
  <p>Choose which subset of outside collaborators will require approval to run workflows on their pull requests.
  <a class="Link--inTextBlock" href="<%= "#{GitHub.help_url}/actions/managing-workflow-runs/approving-workflow-runs-from-public-forks" %>">Learn more about approving workflow runs from public forks.</a></p>

  <%= render(Actions::Policy::ForkPrApprovalsForm.new(f, entity: @entity, action: @action)) %>
<% end %>
