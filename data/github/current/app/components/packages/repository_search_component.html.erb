<div <%= test_selector("repositories-autocomplete") %>>
  <%= render Primer::Beta::AutoComplete.new(
                  label_text: "Repository:",
                  src: packages_two_search_repositories_path(@owner.organization? ? "orgs" : "users", @package.owner.display_login, @package.package_type, @package.name),
                  full_width: true,
                  input_id: "repository-search-repo-autocomplete",
                  list_id: "repository-search-repo-autocomplete-list",
                  input_name: "update_repo[repo_name]",
                  placeholder: "Search by Name",
                  show_clear_button: true
                ) do |c| %>
                  <% c.with_input(form: "packages-repo-form") %>
                <% end %>
</div>
