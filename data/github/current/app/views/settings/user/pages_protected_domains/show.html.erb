<% page_info :title => "Add a Pages verified domain" %>

<%= render layout: "settings/settings" do %>
  <%= render partial: "settings/pages_protected_domains/show", locals: {
    domain: domain,
    update_url: settings_pages_protected_domain_path(domain),
    pages_settings_path: settings_pages_path,
  } %>
<% end %>
