---
summary: Get a repository secret
description: |-
  Gets a single repository development environment secret without revealing its encrypted value.

  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
tags:
  - codespaces
operationId: codespaces/get-repo-secret
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/codespaces/repository-secrets#get-a-repository-secret'
parameters:
  - $ref: '../../components/parameters/owner.yaml'
  - $ref: '../../components/parameters/repo.yaml'
  - $ref: '../../components/parameters/secret-name.yaml'
responses:
  '200':
    description: Response
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/repo-codespaces-secret.yaml'
        examples:
          default:
            $ref: '../../components/examples/repo-codespaces-secret.yaml'
x-github-releases:
  - ghec
  - api.github.com
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: codespaces
  subcategory: repository-secrets
x-github-internal:
  path: '/repos/{owner}/{repo}/codespaces/secrets/{secret_name}'
  http-method: get
  owner: '@github/codespaces-team'
  published: true
