---
summary: Delete a reaction
description: |-
  OAuth app tokens and personal access tokens (classic) need the `write:discussion` scope to use this endpoint
  when deleting a reaction from a [team discussion](${externalDocsUrl}/rest/teams/discussions#get-a-discussion)
  or [team discussion comment](${externalDocsUrl}/rest/teams/discussion-comments#get-a-discussion-comment).
operationId: reactions/delete
tags:
  - reactions
externalDocs:
  description: API method documentation
  url: '${externalDocsUrl}/rest/reactions/reactions#delete-a-reaction'
parameters:
  - name: accept
    description: This API is under preview and subject to change.
    in: header
    schema:
      type: string
      default: application/vnd.github.echo-preview+json
    required: true
  - $ref: '../../components/parameters/reaction-id.yaml'
responses:
  '204':
    description: Response
x-githubEnterpriseOverlays:
  '<= 3.2':
    parameters:
      - name: accept
        description: This API is under preview and subject to change.
        in: header
        schema:
          type: string
          default: application/vnd.github.echo-preview+json
        required: true
    x-github:
      previews:
        - $ref: '../../components/x-previews/squirrel-girl.yaml'
          required: true
        - $ref: '../../components/x-previews/echo.yaml'
          required: true
x-github-releases:
x-github:
  githubCloudOnly: false
  enabledForGitHubApps: true
  category: reactions
  subcategory: reactions
deprecated: true
x-github-internal:
  path: '/reactions/{reaction_id}'
  http-method: delete
  service-tier: 2
  owner: '@github/issues'
  published: true
