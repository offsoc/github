import type {SecurityCenterPaginationProps} from '../SecurityCenterPagination'

export function getSecurityCenterPaginationProps(): SecurityCenterPaginationProps {
  return {
    currentPage: 1,
    pageCount: 2,
  }
}
