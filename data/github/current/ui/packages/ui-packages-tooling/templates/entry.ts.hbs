/**
 * This file automatically registers this package with webpack and creates a bundle named `{{ packageName }}`
 * Any code written in {{ packageName }}.ts will run when the bundle is loaded.
 */
import './{{ packageName }}'
