<% if view.entry.hit[:enforcement] == "enforced" %>
  Enforced <span class="context"><%= view.entry.hit[:security_configuration_name] %></span> in <span class="context"><%= view.entry.safe_organization_name %></span> organization
<% else %>
  Enforcement removed from <span class="context"><%= view.entry.hit[:security_configuration_name] %></span> in <span class="context"><%= view.entry.safe_organization_name %></span> organization
<% end %>
